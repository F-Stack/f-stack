# 审核门禁

## 审核项

### 1. 代码修改审核

| 审核项 | 状态 | 说明 |
|--------|------|------|
| `ff_config.h` 新增字段位置正确 | ✅ PASS | 在 `tx_csum_offoad_skip` 后、`vlan_strip` 前 |
| `ff_config.c` MATCH 分支语法正确 | ✅ PASS | 在 `tx_csum_offoad_skip` 后、`vlan_strip` 前 |
| `ff_dpdk_if.c` IP/L4 条件拆分逻辑正确 | ✅ PASS | 默认值下行为与原代码完全一致 |
| `ff_dpdk_if.c` TSO 警告条件更新 | ✅ PASS | 新增 `tx_csum_l4_skip` 检查，字符串字面量未变 |
| `ff_dpdk_if.c` TX 路径 IP 守卫 | ✅ PASS | `ctx->hw_features.tx_csum_ip && offload.ip_csum` |
| `ff_memory.c` TX 路径 IP 守卫 | ✅ PASS | 与 `ff_dpdk_if.c` 对称 |
| `config.ini` 新配置项注释 | ✅ PASS | 默认注释掉（`#tx_csum_ip_skip=0`） |
| 最小注释规约 | ✅ PASS | 无冗余注释 |
| 无格式抖动 | ✅ PASS | 保持原有缩进、括号风格 |

### 2. 编译验证

| 审核项 | 状态 | 说明 |
|--------|------|------|
| make clean 后编译通过 | ✅ PASS | libfstack.a 6.9MB，无编译错误 |
| 无新增 lint 错误 | ✅ PASS | 4 个修改文件均无 lint 错误 |

### 3. 测试验证

| 测试 | 配置 | TCP 连接 | IP cksum | TCP cksum | 状态 |
|------|------|----------|----------|-----------|------|
| T1 | 默认（skip=0） | ✅ | ✅ | ✅ | PASS |
| T2 | skip=1 | ✅ | ✅ | ✅ | PASS |
| T3 | ip_skip=1 | ✅ | ✅ | ✅ | PASS |

### 4. 向后兼容性

| 配置组合 | 行为 | 兼容性 |
|----------|------|--------|
| 全部默认（0/0/0） | 全部启用 | ✅ 与原代码一致 |
| skip=1 | 全部禁用 | ✅ 与原代码一致 |
| skip=0 + ip_skip=1 | 只禁用 IP | ✅ 新功能 |
| skip=0 + l4_skip=1 | 只禁用 L4 | ✅ 新功能 |

### 5. config.ini 提交约束

| 审核项 | 状态 | 说明 |
|--------|------|------|
| config.ini 本地测试值不入库 | ✅ PASS | 提交前恢复默认值 |
| git diff 只含特性相关改动 | ✅ PASS | 新增注释行为特性相关 |

## 审核结论

**PASS** — 所有审核项通过。代码修改最小化（7 处精确修改），向后兼容，测试验证通过。
