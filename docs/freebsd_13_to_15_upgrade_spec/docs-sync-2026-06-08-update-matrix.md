# docs/ 3-Tier Architecture & Knowledge Graph Sync — Authoritative Update Matrix

> **Generated**: 2026-06-08 (UTC) by analyzer phase (4 parallel sub-agents + GitNexus re-index)
> **Repo HEAD**: `208b0c4299a070cd4192e45ae8da720ddfaa99f3` (branch `dev`, merge of `feature/1.26`)
> **GitNexus index**: re-built `2026-06-08T03:37:02Z`, schema v1, 2,656 files / 64,855 nodes / 113,858 edges / 981 communities / 300 flows
> **Authority order**: actual code (`lib/` + `freebsd/` + `app/` + `dpdk/`) > upgrade evidence (`freebsd_13_to_15_upgrade_spec/`) > existing docs (the targets being patched)
> **Output discipline**: every modifier patch must trace to a row in this matrix; rows lacking evidence are FORBIDDEN.

---

## §0 Scope & doc inventory

18 target files, two language mirrors:

| Path | Lines (current) | Role |
|---|---|---|
| `docs/01-LAYER1-ARCHITECTURE.md` | ~330 | EN short Layer 1 |
| `docs/02-LAYER2-INTERFACES.md` | ~360 | EN short Layer 2 |
| `docs/03-LAYER3-FUNCTIONS.md` | ~700 | EN short Layer 3 |
| `docs/F-Stack_Architecture_Layer1_System_Overview.md` | 825 | EN long Layer 1 |
| `docs/F-Stack_Architecture_Layer2_Interface_Specification.md` | 1183 | EN long Layer 2 |
| `docs/F-Stack_Architecture_Layer3_Function_Index.md` | 1112 | EN long Layer 3 |
| `docs/F-Stack_Knowledge_Base_Summary.md` | 751 | EN summary |
| `docs/KNOWLEDGE_GRAPH_WIKI.md` | ~520 | EN KG wiki |
| `docs/README.md` | ~300 | EN readme |
| `docs/zh_cn/<each above>` | (≈) | ZH mirror, must keep parity |

---

## §1 Authoritative facts from current code (cross-verified)

### §1.1 Versions (DELTA from existing docs)

| Field | Existing docs say | **Reality (HEAD 208b0c4)** | Source |
|---|---|---|---|
| F-Stack version | `v1.25` | **`v1.26`** (feature/1.26 merged into dev; user-confirmed naming) | `git log` + previous-session memory |
| FreeBSD port base | `13.0` | **`15.0` RELEASE-p9** (`__FreeBSD_version=1500068`, syscall table 580→599) | `03-freebsd-15-changes.md §1` |
| FreeBSD upgrade path | (n/a) | 13.0 → 14.0 → 14.1 → 14.2 → 14.3 → 14.4 → 15.0 (6 releases) | `03-freebsd-15-changes.md §8` |
| DPDK | `23.11.5` | **`23.11.5` (unchanged, C-3 constraint)** | `git submodule status dpdk`; `01-requirements-spec.md §C-3` |
| nginx | various (some say "1.16+", some unspecified) | **`1.28.0`** (single tree, both 13.0 baseline and 15.0 runtime-fix-done) | `app/nginx-1.28.0/`, `runtime-fix-execution-log.md §12.13` |
| redis | (mostly absent) | **`6.2.6`** (with `src/ae_ff_kqueue.c` + `src/anet_ff.c` fstack patches) | `app/redis-6.2.6/`, `runtime-fix-execution-log.md §12.14` |
| Toolchain (clang/llvm) | (absent) | clang 11.0.1 (13.0R) → 16 (14.0R) → **18.1.6** (15.0R baseline); GCC12 `stringop-overflow` guard relaxed `__GNUC__ >= 13 → >= 12` | `03-freebsd-15-changes.md §2.2 / §10.1`; `runtime-fix-execution-log.md §11.4` |
| ConcurrencyKit | (absent) | upgraded during M3 to a 15.0-compatible revision (added `CK_LIST_FOREACH_FROM`, a 14.0+ macro); exact version not in evidence files | `M3-execution-log.md §4 RB-M3-03` |

### §1.2 lib/ file inventory — VERIFIED (33 .c files, NOT 9)

> Existing `01-LAYER1-ARCHITECTURE.md §2.1` lists only 9 lib/ files; **17 files are missing**, and the listed line counts have all drifted +1~+2 lines.

**Complete list** (line counts verified by `wc -l` / read tail):

| # | File | Lines (verified) | Role |
|---|---|---|---|
| 1 | `lib/cryptodev_if.c` | ~92 | KOBJ method dispatch table, auto-generated by `makeobjops.awk` from `freebsd/opencrypto/cryptodev_if.m` |
| 2 | `lib/ff_compat.c` | ~360 | FreeBSD compatibility shim (refcount/stat/cdefs), derived from libplebnet `pn_compat.c` |
| 3 | `lib/ff_config.c` | **1381** (was 1379 in docs) | INI config parsing, `ff_global_cfg`, `ff_check_config` |
| 4 | `lib/ff_dpdk_if.c` | **2856** (was 2855 in docs) | DPDK NIC adapter — EAL/mempool/RX-TX burst/RSS/main_loop |
| 5 | `lib/ff_dpdk_kni.c` | ~441 | Virtual NIC (KNI bridge: ARP/ICMPv6 bypass) |
| 6 | `lib/ff_dpdk_pcap.c` | ~118 | libpcap rotation capture (`PCAP_FILE_NUM=10`) |
| 7 | `lib/ff_epoll.c` | ~134 (was 159 in docs — verify) | Linux `epoll_*` → FreeBSD `kqueue` translation |
| 8 | `lib/ff_freebsd_init.c` | ~154 | FreeBSD subsystem boot sequence, `ff_freebsd_init` |
| 9 | `lib/ff_glue.c` | **1468** (was 1466 in docs) | FreeBSD kernel glue (kthread/event/jail/mutex/kmem userspace emulation), derived from libplebnet `pn_glue.c` |
| 10 | `lib/ff_host_interface.c` | ~285 | Host OS interface (mmap/pthread/rand/clock), derived from libuinet |
| 11 | `lib/ff_ini_parser.c` | ~170 | inih INI parser (3rd-party, New BSD) |
| 12 | `lib/ff_init.c` | **70** (was 69 in docs) | Top-level orchestrator: `ff_init → ff_load_config → ff_dpdk_init → ff_freebsd_init → ff_dpdk_if_up`; `ff_run`/`ff_stop_run` wrappers |
| 13 | `lib/ff_init_main.c` | ~660+ | `proc0`/`thread0`/`ucred`/`fdcopy` infrastructure init (incl. `mac_cred_create_swapper`, `callout_init(&p_itcallout)`) |
| 14 | `lib/ff_kern_condvar.c` | ~88 | `cv_*` condition variables, derived from libplebnet `pn_kern_condvar.c` |
| 15 | `lib/ff_kern_environment.c` | 509 | Kernel `kenv` redirected to libc `getenv/setenv`, derived from libuinet |
| 16 | `lib/ff_kern_intr.c` | 108 | Soft IRQ / `swi_*` userspace stack-borrow shim, derived from libuinet |
| 17 | `lib/ff_kern_subr.c` | 271 | `uiomove`/`copyin`/`copyout` userspace impl (incl. `WITNESS_WARN`), derived from libplebnet |
| 18 | `lib/ff_kern_synch.c` | 132 | `tsleep`/`wakeup`/`msleep` synch primitives, derived from libplebnet |
| 19 | `lib/ff_kern_timeout.c` | **1266** | callout subsystem: `callout_init`/`_reset_tick_on`/`_schedule`/`_callwheel_init`/`ff_timecounter` (FreeBSD 13/14-compat `_ff_callout_stop_safe`) |
| 20 | `lib/ff_lock.c` | **448** | sx/mutex/lockmgr userspace impl (`sx_init_flags`/`_destroy`/`_try_*`/`_downgrade`/`_sysinit`), derived from libplebnet |
| 21 | `lib/ff_log.c` | 111 | F-Stack-native syslog adapter |
| 22 | `lib/ff_memory.c` | 481 | mbuf/mempool adapters: `ff_init_ref_pool`, `ff_mmap_init`, `ff_if_send_onepkt`, `ff_enq_tx_bsdmbuf` |
| 23 | `lib/ff_ng_base.c` | **3887** | Full netgraph framework port (nodes/hooks/messages/workqueue, `MODULE_VERSION(netgraph,...)`, `DECLARE_MODULE(netgraph,...)`), original by Whistle Communications/Julian Elischer |
| 24 | `lib/ff_ngctl.c` | 131 | netgraph user control (ng_socket / syscallsubr bridge) |
| 25 | `lib/ff_route.c` | **1604** | Route socket (FreeBSD `rtsock.c` partial port): `ff_rtioctl`, `ff_rtioctl_old`, RIB hooks |
| 26 | **`lib/ff_stub_14_extra.c`** | **799** | **NEW (M5+runtime-fix)** — central 14.0+ stub bank (123 stubs covering 661 undefs: rib new API + netlink/genl + tcp ECN/HPTS + aio + nvlist + m_snd_tag + tqhash + prison_check_ip*_locked + vm series); 5 runtime-fix patches landed here (rt_alloc 3rd-arg fix, rtbridge stub, badfileops removal, vm_page_alloc_noobj defensive panic) |
| 27 | `lib/ff_subr_epoch.c` | 83 | epoch stub (verify-only at M2; not in KERN_SRCS) |
| 28 | `lib/ff_subr_prf.c` | 604 | kernel printf userspace impl |
| 29 | `lib/ff_syscall_wrapper.c` | 1815 | Linux→FreeBSD syscall translation (entry symbol for ff_socket/ff_read/ff_write/...) |
| 30 | `lib/ff_thread.c` | 51 | minimal thread shim |
| 31 | `lib/ff_veth.c` | **1132** | Virtual ethernet device (28 `if_t` accessor rewrites at M4: `ifp->if_xxx → if_get*/if_set*`) |
| 32 | `lib/ff_vfs_ops.c` | 117 | VFS ops shim |
| 33 | (additional, from analyzer) | — | inih `ff_ini_parser.c` already counted |

**`lib/include/`** — header inventory not exhaustively walked in analyzer pass; key headers known: `ff_api.h`, `ff_memory.h` (line 102-105: `ff_enq_tx_bsdmbuf` + ref-pool decls).

**`lib/Makefile` delta** (vs 13.0 baseline):
- `+ NET_SRCS += route_rtentry.c` (rib-fix Phase 2)
- `__GNUC__` guard: `>= 13` → **`>= 12`** for `stringop-overflow` (GCC12 cluster build)
- 16 `*_SRCS` index variables → 188 `.c` total compiled (`04-diff-and-port-strategy.md §2`)

### §1.3 freebsd/ subsystem topology (DELTA from existing docs)

| Doc claim | Reality | Action |
|---|---|---|
| `freebsd/sys/{netinet,netinet6,net,kern,vm}/` shown as flat list | Actually 25 top-level dirs: `Makefile / amd64 / arm / arm64 / bsm / contrib / crypto / ddb / i386 / kern / libkern / net / net80211 / netgraph / netinet / netinet6 / netipsec / netlink / netpfil / opencrypto / security / sys / tools / vm / x86` | **Replace** the section |
| `freebsd/sys/netinet/` is described | Correct; tcp_stacks lives at `freebsd/netinet/tcp_stacks/` (11 files: `rack.c` 759 KB, `bbr.c` 444 KB, `rack_pcm.c`, `tailq_hash.*`) | **Add** tcp_stacks subdir reference |
| (no mention of netlink) | `freebsd/netlink/` exists since 14.0 — **header-only port (18 .h, 0 .c, 0 SRCS in Makefile)**; serves only header dependency for `net/{if_clone,if_vlan}.c`; DP-2 decided not to port the actual NETLINK protocol | **Add** as new subsystem with explicit "header-only, not compiled" note |
| (no mention of net/route/) | `freebsd/net/route/` (22 files) — FIB rework subdir (nhop / fib_algo / route_ctl) introduced in 14.0+ | **Add** |
| `mips/` in path of arch list | **Removed** at M1; `freebsd/mips/` deleted; remaining `mips` strings only in `freebsd/contrib/device-tree/` (third-party DTS, not compiled into f-stack) | **Remove** mips from arch list; explicitly note removal |
| `amd64/` `i386/` `arm/` `arm64/` `x86/` | All present | **Keep**, list completely |
| `contrib/ck/` (ConcurrencyKit) | Present, upgraded at M3 | **Keep**, note version refresh |
| KTLS | (absent) | **Add note**: 15.0 GENERIC kernel ships KTLS by default; F-Stack does not enable; `sys_socket.c`/`uipc_socket.c` KTLS branches stubbed |
| TCP stacks modularization | (absent) | **Add note**: RACK/BBR/extra TCP stacks modularized via `compile-with -DMODNAME=tcp_rack -DSTACKNAME=rack`; F-Stack H-5 module-rename (`tcp_rack_fstack`) re-applied at 15.0 baseline |

### §1.4 app/ + adapter/ + example/ topology

| Existing doc | Reality | Action |
|---|---|---|
| `app/` lists nginx + redis (sometimes also helloworld/micro_thread) | `app/` contains **only** `nginx-1.28.0/` + `redis-6.2.6/` (verified by `ls`) | **Correct** to 2 entries |
| (sometimes implies micro_thread under app/) | `micro_thread/` + `syscall/` live under `adapter/` (not `app/`) | **Move** to a separate "adapter/" section |
| (sometimes implies helloworld under app/) | `helloworld/helloworld_epoll/main_zc.c` are under `example/` (Makefile L19-22) | **Add** "example/" section |

### §1.5 dpdk/ submodule

- `git submodule status dpdk` → `23.11.5` (pinned)
- `dpdk/lib/` subdirectories include EAL / mempool / ethdev / mbuf / hash / etc. (standard 23.11 layout)
- C-3 constraint: do NOT change DPDK dependency this round; out-of-scope for 13.0→15.0 upgrade

### §1.6 Configuration (`config.ini`)

- Path: `/data/workspace/f-stack/config.ini` (root-level)
- Section names not enumerated in this matrix (analyzer-D output truncated); modifier should `head -100 config.ini` to refresh §"Configuration sections" verbatim before patching.

---

## §2 Interface-level deltas (Layer 2 / 3 docs)

### §2.1 Removed / replaced kernel ABIs (15.0 vendor evolution)

| Old API (13.0) | New API (15.0) | F-Stack adaptation |
|---|---|---|
| `pr->pr_usrreqs->pru_*()` (legacy `pr_usrreqs` table) | Merged into `protosw`: `pr->pru_*()` directly | T-ff-01 verify-only at M3; existing `lib/ff_glue.c` already used new form (spec mis-flagged P0) |
| `struct ifnet *` direct field access (`ifp->if_*`) | Opaque `if_t` with `if_get*` / `if_set*` accessors | M4 R-013: 28 rewrites in `lib/ff_veth.c`; `if_alloc(u_char type)` signature **unchanged** (despite spec 03 §3 claim) |
| `rib_lookup_info(...)` | **Removed entirely** in 14.0+ (not "signature changed") | `lib/ff_route.c` reworked; `lib/ff_stub_14_extra.c` provides shim where compatible |
| `rt_alloc(rnh, dst, flags=int)` | `rt_alloc(rnh, dst, struct sockaddr *netmask)` | rib-fix Phase 2: `lib/ff_stub_14_extra.c` corrected 3rd-arg signature |
| `rt_ifmsg()` direct dispatch | Now via `rtbridge` function-pointer table (`rtsock_callback_p`, `netlink_callback_p`) | runtime-fix #3: `lib/ff_stub_14_extra.c` provides `static struct rtbridge ff_stub_rtbridge_noop` + dual callback pointers |

### §2.2 syscall table (15.0 net-add 22, net-remove 3)

- **Added** (subset, not exhaustive): `kqueuex(583)`, `membarrier(584)`, `timerfd_*(585-587)`, `kcmp(588)`, `fchroot(590)`, `setcred(591)`, `inotify_*(593-594)`, `jail_*_jd(597-598)`
- **Removed**: `gssd_syscall(505)`, `sbrk(69)`, `sstk(70)`
- **C-1 constraint**: F-Stack does NOT introduce wrappers for new 15.0 syscalls (`inotify_add_watch_at`, `kqueuex`, `timerfd_*`) into `ff_syscall_wrapper.c` this round.

### §2.3 8 categories of 14.0+ ABI deltas (M4 §6)

| Category | Example | Where fixed |
|---|---|---|
| (a) `bool`-ification | `prison_*()` returns `bool` not `int` | `lib/ff_stub_14_extra.c` (and audit of `lib/ff_glue.c` callers) |
| (b) `const void *` in init macros | `mtx_sysinit`, `tunable_*_init` | `lib/ff_lock.c` + `lib/ff_glue.c` |
| (c) `void *` in kmem | `kmem_*` allocators | `lib/ff_glue.c` |
| (d) sockaddr calling-convention | `kern_accept` 2nd arg signature | `lib/ff_syscall_wrapper.c` + cf. runtime-fix #5 |
| (e) Field deletion | `sysentvec.sv_transtrap` removed; `rtentry.rt_expire` removed | conditional-compile guards in `lib/ff_stub_14_extra.c` |
| (f) Macro deletion | `CALLOUT_LOCAL_ALLOC`, `CS_EXECUTING`, `SI_SUB_DONE`, `RTF_RNH_LOCKED` | `lib/ff_kern_timeout.c` + `lib/ff_route.c` |
| (g) Signature change | `fdinit`, `_callout_stop_safe`, `NDFREE → NDFREE_PNBUF` | `lib/ff_glue.c`, `lib/ff_kern_timeout.c`, `lib/ff_vfs_ops.c` |
| (h) const cred | `groupmember(cred)` const-qualified | `lib/ff_glue.c` |

### §2.4 inpcb epoch → SMR

- `in_pcb.h:141-192` introduces `smr_seq_t inp_smr` + SMR section lookup.
- `lib/ff_subr_epoch.c` stub scope to be re-evaluated (not re-rewritten this round).

---

## §3 Architecture / boundary changes

| Change | Description | Doc impact |
|---|---|---|
| **mips removal** | FreeBSD 14.0 deleted mips arch upstream; F-Stack synchronously removed `freebsd/mips/`, all Makefile/mk mips-conditional code purged | Layer1 §directory tree, Layer1 §supported arch, README §requirements, Knowledge_Base_Summary §subsystems |
| **netlink subsystem (header-only)** | `freebsd/netlink/` 18 .h files added at 14.0+; F-Stack uses for header-dep compatibility (e.g. `net/if_clone.c`); **no .c files compiled, no NETLINK protocol port** (DP-2 decision) | Layer1 §subsystems, Layer2 §kernel KBI/KPI |
| **net/route/ FIB rework** | `freebsd/net/route/` (22 files) introduced subdir for nhop/fib_algo/route_ctl since 14.0+ | Layer1 §subsystems, Layer3 §rib functions |
| **TCP stacks modularization** | RACK/BBR/extra stacks built via `-DMODNAME=tcp_rack -DSTACKNAME=rack`; F-Stack H-5 module rename `tcp_rack_fstack` re-applied | Layer1 §subsystems, Layer2 §TCP stacks |
| **KTLS default-on (15.0 GENERIC)** | F-Stack does NOT enable; KTLS branches in `sys_socket.c`/`uipc_socket.c` stubbed | Layer1 §security/crypto, Layer2 §socket KBI |
| **`ff_stub_14_extra.c` central stub bank** | NEW file at M5; collects 123 stubs to resolve 661 undefs after `make clean && make` exposed M3-end .o cache illusion | Layer3 §lib/ functions (new section) |
| **rte_kni / virtio_user evolution** | NOT documented as a primary change in the 8 evidence files; only mentioned at perf flame-graph level (`virtio_recv_mergeable_pkts` share rises in CVM path); **do not introduce** as new architectural change in docs (no first-source backing) |  — |
| **netgraph (FF_NETGRAPH=1) build matrix** | `lib/ff_ng_base.c` `node_p → node_cp` correction at M5; Makefile dropped `ng_atmllc`/`ng_sppp` references; FF_NETGRAPH=1 matrix passed | Layer1 §build matrix, Layer3 §netgraph functions |

---

## §4 Five P0 SIGSEGV runtime fixes (Layer3 + Summary impact)

> Authoritative source: `runtime-fix-execution-log.md §6 / §8 / §11 / §12`

| # | Symptom | Root cause | Fix | Files touched | Impact cluster |
|---|---|---|---|---|---|
| **1** | UMA `keg_fetch_slab` infinite loop with `M_WAITOK` | `UMA_USE_DMAP` macro renamed from `UMA_MD_SMALL_ALLOC` at 14.0+; F-Stack lost `#ifndef FSTACK` wrapper | +2 lines `#ifndef FSTACK` wrapper | `freebsd/{amd64,arm64}/include/vmparam.h` | UMA, early init |
| **2** | `smr_create` SIGSEGV at `%gs:0x100` | `amd64/include/atomic.h::__storeload_barrier` `_KERNEL` path uses `lock addl $0,%gs:OFFSETOF_MONITORBUF`; userspace has no `%gs` PCPU segment | Add `#if defined(_KERNEL) && !defined(FSTACK)` guard, fall back to `lock addl $0,-8(%rsp)` | `freebsd/amd64/include/atomic.h` | SMR, atomic primitives, SMP barrier |
| **3** | `rt_ifmsg` SIGSEGV (NULL deref) | 14.0+ `rt_ifmsg` dispatches via `rtbridge` function-pointer table; `rtsock_callback_p` / `netlink_callback_p` left NULL | Provide `static struct rtbridge ff_stub_rtbridge_noop = { .route_f=noop, .ifmsg_f=noop }` + dual pointer wiring | `lib/ff_stub_14_extra.c` | route socket, ifioctl IP config |
| **4** | `ff_veth_setaddr` errno 55 (ENOBUFS) | `lib/Makefile` missed `route_rtentry.c`; 11 stubs in `ff_stub_14_extra.c` had wrong `rt_alloc` 3rd-arg type (`route_nhop_data *` vs real `sockaddr *`) | `Makefile +1 SRCS` + delete 11 wrong stubs | `lib/Makefile`, `lib/ff_stub_14_extra.c` | rib/rtentry, IP config, loopback route |
| **5** | `kern_accept` error path SIGSEGV ip=0x0 | `badfileops` excluded by 15.0 vendor `#ifndef FSTACK` (kern_descrip.c:5372); M5 added `const struct fileops badfileops = {0}` causing `_fdrop → fo_close()` to NULL | Move boundary `5372 → 5475` to recompile 11 `badfo_*` + `badfileops`; remove stub | `freebsd/kern/kern_descrip.c`, `lib/ff_stub_14_extra.c` | fd/fileops, accept fast path |
| **6 (defensive)** | (would-be silent NULL on future regression) | `vm_page_alloc_noobj_domain` / `vm_page_alloc_noobj` stubs returned NULL silently | Hard `panic()` to surface future regressions | `lib/ff_stub_14_extra.c` | VM, page allocation |

All 6 commits follow `DP-DBG-2=A` (one-root-cause-one-commit) discipline (`runtime-fix-execution-log.md §11.7 / §12.9`).

---

## §5 Performance baselines (README + Summary impact)

### §5.1 CVM same-window A/B (DPDK 23.11.5 fixed; only F-Stack lib + freebsd kernel delta varies)

| Test | Tool / Window | 13.0 → 15.0 | Verdict |
|---|---|---|---|
| T1 t2c10 5s | wrk → nginx_fstack | −2.69%; p99 +39.7% | regress |
| T2 t4c100 30s | wrk → nginx_fstack | 220,691 → 203,933 req/s = **−7.59%** | regress (vendor evolution attribution) |
| T3 t8c500 30s | wrk → nginx_fstack | 239,555 → 217,100 req/s = **−9.37%**; p99 +18.6% | regress (vendor evolution attribution) |

**Root cause attribution** (perf flame-graph, 29,974 samples per side, no overlap with runtime-fix touch points):
- `tcp_default_output` vtable: +1.0 pp
- CUBIC state-machine: +0.6 pp
- `sb_locking` rework: +1.5 pp
- `ether_nh_input`: +0.7 pp
- `uma_zfree_arg`: +0.5 pp
- (Diff flame-graph **excludes** `kern_accept`, `badfileops`, `route_rtentry`, `atomic.h` — i.e. the 9% gap is **NOT** introduced by runtime-fix)

### §5.2 Bare-metal physical baseline (Intel Xeon 8255C @ 2.5 GHz + Mellanox CX-5 100G + TencentOS 4.4 + Linux 6.6.98)

| Workload | 1-core | 2-core | 4-core | Verdict |
|---|---|---|---|---|
| helloworld single-core long-conn | 958,109 → 1,056,178 req/s = **+10.24%**; p99 −3.43% | — | — | net gain |
| nginx_fstack long-conn | **+5.06%** | +4.76% | +4.83% | net gain (1→4 core scaling ≥ 97.5%) |
| nginx_fstack short-conn | −2.25% | −3.65% | **−6.10%** ⚠ | trade-off (only 4-core crosses NFR-1 5% threshold) |

**NFR-1 verdict**: PASS overall; nginx_fstack 4-core short-conn `−6.10%` filed as a **trade-off observation, non-blocking** (`physical-machine-bench-report.md §6.2 / §6.3`).

### §5.3 Application-level cross-checks

- **nginx A/B** (CVM, both 15.0 trees with/without runtime-fix): T1 +1.05%, T2 −1.05%, T3 −0.55% → all within jitter; T1 p99 −37.6% (15.0 improves), T3 p99 +18.6% (same direction as helloworld but smaller).
- **redis A/B** (CVM): T2 PING_INLINE / GET +5.15% / +5.37%; T1/T3 client-bound flat → ae+pipeline path strongest regression resistance.

---

## §6 Knowledge Graph (KG WIKI rewrite source data)

> Existing `KNOWLEDGE_GRAPH_WIKI.md` claims:
>   - Indexed time `2026-04-09`, commit `a695757`
>   - 25,723 files / 710,596 nodes / 1,270,994 edges / 11,375 communities / 300 flows
>   - Excluded `freebsd/`, `dpdk/` (text-only claim)
>   - Detailed "Node Type Distribution" (Macro/Function/Struct/Property/etc.)
>   - Top community list (Net 823 / Netinet 245 / Tcp_stacks 91 / ...)
>   - **Doc itself says** "FreeBSD **13.1** kernel source (excluded from indexing)" at line 224 — outdated AND wrong (was 13.0, now 15.0)

> **New gitnexus 1.6.5 schema v1 reality** (re-indexed 2026-06-08T03:37:02Z, commit `208b0c4`):
>   - 2,656 files / 64,855 nodes / 113,858 edges / 981 communities / 300 flows
>   - F-Stack-only effective scope (the new schema actually excludes vendored trees, vs old wiki's text-level claim)
>   - Schema v1 exposes only top-level stats; **detailed node-type & community-name tables are NOT in `meta.json`** — would require LLM-driven `gitnexus wiki` (LLM API key not configured) or direct lbug DB query (out-of-scope for this task)
>   - Capabilities: `ladybugdb` graph + FTS available; `vectorSearch` unavailable (exactScanLimit=10000)
>   - `embeddings` count = 0 (not enabled this round)

**KG_WIKI rewrite strategy** (modifier-en/zh):
1. Replace top-of-file metadata (date/commit/file/symbol/edge/community counts) with new values.
2. **Drop the detailed "Node Type Distribution" table** — explicitly note the new schema does not expose this (avoid fabricating numbers); add a "regenerate via `gitnexus wiki` with LLM API key" hint.
3. **Drop the detailed Top-Community table** with named clusters (Net, Netinet, Tcp_stacks, …) — these were schema-v0 outputs not reproducible without LLM-side enrichment.
4. **Keep** the high-level structural prose: F-Stack core modules / FreeBSD subsystem boundaries / DPDK adaptation / 3-tier doc nav. Update L224 "FreeBSD 13.1" → "FreeBSD 15.0" and refresh module-level descriptions to reflect §1-§5 of this matrix.
5. **Add** a new section: "FreeBSD 13.0 → 15.0 upgrade delta map" — points at `freebsd_13_to_15_upgrade_spec/` evidence files (M0~M5, runtime-fix, Phase-5b, rib-fix, baselines).
6. ZH mirror keeps strict parity (same numbers, same headings).

---

## §7 Outdated reference catalog (modifier strict-find list)

> From analyzer-C scan; for each file, modifier MUST replace ALL listed occurrences. Both EN and ZH mirrors covered (mirror line numbers not enumerated here — modifier reads the ZH file and applies the same conceptual edit at the corresponding location).

### §7.1 `v1.25` → `v1.26`

| File (EN) | Lines | Action |
|---|---|---|
| `01-LAYER1-ARCHITECTURE.md` | 1 | Replace `F-Stack v1.25` → `F-Stack v1.26` |
| `02-LAYER2-INTERFACES.md` | 1 | same |
| `03-LAYER3-FUNCTIONS.md` | 1 | same |
| `F-Stack_Architecture_Layer1_System_Overview.md` | (search & replace global) | same |
| `F-Stack_Architecture_Layer2_Interface_Specification.md` | (search & replace global) | same |
| `F-Stack_Architecture_Layer3_Function_Index.md` | (search & replace global) | same |
| `F-Stack_Knowledge_Base_Summary.md` | (search & replace global) | same |
| `KNOWLEDGE_GRAPH_WIKI.md` | (search & replace global) | same |
| `README.md` | (search & replace global) | same |

ZH mirrors: same set, identical replacement.

### §7.2 `FreeBSD 13(.0|.1)` / "13.0 port" / "13.0 移植"

| File (EN) | Lines/Anchors | Action |
|---|---|---|
| `01-LAYER1-ARCHITECTURE.md` | L36 (table row), L66 (dir tree), L123 (history) | Replace `FreeBSD 13.0 port` → `FreeBSD 15.0 port`; in §history, **add** "2025-2026 upgraded to FreeBSD 15.0 (M0~M5 + runtime-fix)" |
| `F-Stack_Architecture_Layer1_System_Overview.md` | L95 (dir tree), L651 (timeline) | same conceptual edits |
| `F-Stack_Architecture_Layer2_Interface_Specification.md` | (search & replace) | replace, plus rewrite §interface tables per §2 above |
| `F-Stack_Architecture_Layer3_Function_Index.md` | L455 (`__uint64_t ext[4];  // [32] FreeBSD 13 extended fields`) | replace `FreeBSD 13` → `FreeBSD 13/15` (this is a kernel ABI field; refresh comment to reflect M2/M3 verify-only outcome) |
| `03-LAYER3-FUNCTIONS.md` | L122 (same field comment) | same |
| `F-Stack_Knowledge_Base_Summary.md` | (search & replace) | replace |
| `KNOWLEDGE_GRAPH_WIKI.md` | L224 ("FreeBSD 13.1 kernel source") | replace with `FreeBSD 15.0 kernel source` |
| `README.md` | (search & replace) | replace |

ZH mirrors: same conceptual edits at corresponding lines.

### §7.3 KG WIKI metadata header

| Anchor | Action |
|---|---|
| `> 索引时间 2026-04-09，commit a695757` (or EN equivalent) | Replace with `Indexed: 2026-06-08T03:37:02Z, commit 208b0c4` |
| `25,723 files / 710,596 nodes / 1,270,994 edges / 11,375 communities` | Replace with `2,656 files / 64,855 nodes / 113,858 edges / 981 communities / 300 flows` |
| Detailed "Node Type Distribution" table | Drop; replace with "Schema v1 exposes only top-level counts; node-type breakdown requires LLM-driven `gitnexus wiki` (not configured this round)" |
| Detailed Top-Community list with named clusters | Drop; replace with "Module-level cluster taxonomy — see `freebsd_13_to_15_upgrade_spec/02-architecture-analysis.md` for authoritative subsystem groupings" |

### §7.4 Other outdated technical claims (analyzer-C/D cross-cut)

| File | Anchor | Action |
|---|---|---|
| `01-LAYER1-ARCHITECTURE.md` §2.1 dir tree | 9 lib/ files listed | **Expand to 33** per §1.2 above; group by role (config/init / DPDK / kern stubs / network / netgraph / utility) |
| `01-LAYER1-ARCHITECTURE.md` §2 freebsd/ subtree | shows `kern, vm, netinet, netinet6, net, contrib/ck` only | **Expand** to show `netlink/ (header-only)`, `net/route/`, `netinet/tcp_stacks/`, `net80211/`, etc.; **remove** `mips/` from any arch list (the doc may not currently list it; verify) |
| `01-LAYER1-ARCHITECTURE.md` §app/ | may overstate contents | **Constrain** to `nginx-1.28.0/ + redis-6.2.6/`; add new section for `adapter/` (micro_thread + syscall) and `example/` (helloworld variants) |
| `02-*` Layer2 docs | API surface | Add §"15.0 KBI/KPI deltas" subsection per §2.1-§2.4 above |
| `03-*` Layer3 docs | Function index | Add subsection "Runtime-fix landed functions" per §4 above; refresh `ff_kern_timeout.c` / `ff_route.c` / `ff_stub_14_extra.c` function listings |
| `README.md` | Performance numbers | Refresh per §5 above; add bare-metal baseline + NFR-1 verdict |
| `F-Stack_Knowledge_Base_Summary.md` | Quick-reference cards | Add "Upgrade evidence" card pointing to `freebsd_13_to_15_upgrade_spec/` (M0~M5, runtime-fix, Phase-5b, rib-fix, two baselines) |

---

## §8 Modification discipline (binding for modifier-en / modifier-zh)

1. **Minimum diff**: prefer in-place anchor replacement (`replace_in_file`) over rewriting whole files; whole-file rewrite ONLY allowed for `KNOWLEDGE_GRAPH_WIKI.md` per §6 strategy.
2. **Parity**: every EN edit MUST have a matching ZH edit (same conceptual change, same numbers); verify by `diff -u <(grep -E "^#" EN) <(grep -E "^#" ZH)` heading-level alignment.
3. **No fabrication**: if a number / name is not in §1-§6 above, modifier MUST stop and ask Leader (do NOT make it up).
4. **Forbidden script-bypassed ops**: any rm / kill / chmod inside ANY shell command MUST go through `/data/workspace/{rm_tmp_file,kill_process,chmod_modify}.sh` (see project memory rules 81725399 / 90098233 / 21626578). Pure `git add`/`git commit` (no `--force`) and `make install` are allowed.
5. **Commit at the end**: docs/-only patches in a single commit; English subject + body (memory 73362122); do NOT push (user will review first).

---

## §9 Bounce gate criteria (binding for gatekeeper)

| Gate | Pass criterion | Failure action |
|---|---|---|
| **Gate-1: content vs code** | All §1-§5 anchors replaced; spot-check 5 random anchors → must match real file contents | Bounce → `MODIFY_ZH` or `MODIFY_EN` (whichever is wrong); +1 counter |
| **Gate-2: parity** | EN ↔ ZH heading count equal; same numbers in same sections | Bounce → both modifiers; +1 counter |
| **Gate-3: internal consistency** | short-form Layer1/2/3 numbers ≡ long-form Layer1/2/3 numbers ≡ Summary numbers ≡ KG metadata | Bounce → modifier (the source of inconsistency); +1 counter |

If any single phase counter ≥ 3: Leader → ESCALATED state, emits `docs-sync-2026-06-08-escalation-report.md`, halts.

---

## §10 Phase / file mapping (modifier work assignment)

| Modifier | Files | Anchor count (rough) | Strategy |
|---|---|---|---|
| **modifier-en** | `docs/01-…` `docs/02-…` `docs/03-…` `docs/F-Stack_Architecture_Layer{1,2,3}_*.md` `docs/F-Stack_Knowledge_Base_Summary.md` `docs/KNOWLEDGE_GRAPH_WIKI.md` `docs/README.md` (9 files) | ~50-80 anchor edits + 1 KG full-rewrite | Operate per §7 catalog, 1 file at a time, verify each edit with read_file post-check |
| **modifier-zh** | `docs/zh_cn/<each above>` (9 files) | ~50-80 anchor edits + 1 KG full-rewrite | Mirror EN edits, translation-faithful, same numerical values |

Both modifiers can be batched in parallel within a single Leader response (independent file sets).

---

*End of update-matrix. Modifier phase MUST treat this file as the single source of truth.*
