# F-Stack 13.0 baseline vs 15.0 runtime-fix-done — CVM 同时序压测报告

> **报告时间**：2026-06-03 15:08-15:14（UTC+8）
> **测试主机**：腾讯云 CVM（NUMA 1 节点，dpdk virtio_net 驱动 PCI 00:09.0）
> **F-Stack server**：192.168.1.1（本机）
> **wrk client**：`f-stack-client` 别名（192.168.1.3:22，root@VM-1-3-tencentos）
> **关联文档**：`runtime-fix-execution-log.md` §12.10（双份落档）

---

## 1. 测试目标

回答两个遗留问题：

1. **§12.6 既有 15.0 单边数据（226k/231k req/s）是否已被同硬件 13.0 老基线公平对照？** —— 此前未做，无 13.0 老基线作 sanity reference。
2. **runtime-fix 6 个 commit（5 P0 修复 + 1 防御性）是否引入隐性性能 regression？** —— 此前缺同时序 A/B。

## 2. 测试方法 & 公平性论证

### 2.1 同时序 A/B

13.0 baseline 与 15.0 runtime-fix-done 在**同一台 CVM 同分钟（15:08-15:13）连续切换**，消除跨日网络/温度/CPU 频率漂移影响。两版 helloworld 各跑同三场景，全程 ≈ 6 分钟。

### 2.2 公平变量

| 变量 | 13.0 baseline | 15.0 runtime-fix-done | 一致性 |
|---|---|---|---|
| dpdk 版本 | 23.11.5（系统已装） | 23.11.5（同份） | ✅ 完全一致（pkg-config --modversion 验证） |
| dpdk lib install 路径 | `/usr/local/lib64/` | 同 | ✅ 同份 .so 文件 |
| config.ini | `lcore_mask=10` 单 lcore CPU#4 | 同 | ✅ `diff` exit 0 |
| Port 配置 | port0 192.168.1.1/21, idle_sleep=20, tso=0 | 同 | ✅ 同 config.ini |
| example/Makefile | `-L${FF_PATH}/lib -Wl,--whole-archive,-lfstack` | 同 | ✅ `diff` exit 0 |
| F-Stack 监听端口 | 80 (htons in main.c) | 同 | ✅ 同源 main.c |
| wrk client | f-stack-client `/tmp/wrk/wrk` 同实例 | 同 | ✅ 同 ssh 会话连续触发 |
| wrk 参数 | T1/T2/T3 同 §12.6 | 同 | ✅ 沿用历史参数 |
| 编译类型 | `-O0 -g -gdwarf-2` | 同 | ✅ Makefile 一致 |

### 2.3 唯一变量（Δ source）

唯一差异 = **F-Stack lib + freebsd kernel 源码差**（13.0 vs 15.0 vendor + 6 runtime-fix patch）。

## 3. 编译过程

### 3.1 13.0 baseline 编译

```text
FF_PATH=/data/workspace/f-stack-13.0-baseline
PKG_CONFIG_PATH=/usr/lib64/pkgconfig:/usr/local/lib64/pkgconfig:/usr/lib/pkgconfig
cd $FF_PATH/lib && make -j$(nproc)         # 10s, 191 .o, libfstack.a 4.9MB
cd $FF_PATH/example && make -j$(nproc)     # 2s,  helloworld 27.9MB
```

### 3.2 编译事件：`ff_api.symlist` 误判

**首次 `make -j$(nproc)` 失败**：

```text
make: *** No rule to make target 'ff_api.symlist', needed by 'libfstack.a'.  Stop.
```

**根因**：preflight 阶段（Step 1）我用 `find -name '*.symlist'` 收集"产物"传给 `rm_tmp_file.sh`，但 `ff_api.symlist`（1148 字节，81 行符号导出列表）实际是**手工维护源文件**（lib/Makefile L673 把它列为 libfstack.a 前置依赖）。

**恢复**：rm_tmp_file.sh 是 mv→trash 设计（非真删），从 `/data/workspace/.trash/20260603-065813-1727226/data/workspace/f-stack-13.0-baseline/lib/ff_api.symlist` 用 `cp -p` 复原（sha256 `b70dfbca…3f5c523`，与主树和 runtime-fix-done 树完全一致），随后重编成功。

**经验教训**：`*.symlist` 不能盲目当产物清理；后续清理脚本需 whitelist 明确扩展名（仅 `.o` + `libfstack.a`）。

### 3.3 编译产物双备份

| 用途 | 路径 | sha256 | 大小 |
|---|---|---|---|
| 13.0 baseline binary | `/data/workspace/f-stack-13.0-baseline/example/helloworld_13baseline` | `5b6df6d3312392ff0f7a193d661543b7329883815e587774eb1f71929ef53ad5` | 27,934,872 B |
| 15.0 runtime-fix-done binary | `/data/workspace/f-stack/example/helloworld_15rfix` | `4e3f3c75bc0145864272a5fb470bb90615d9818df28f3faa0b7fc014fb53c3b9` | 28,263,952 B |
| 13.0 libfstack.a | `/data/workspace/f-stack-13.0-baseline/lib/libfstack.a` | `429507d9922a0ff30a10afc424b41a45197fb86f0ebf35f4dda5b4034375051d` | 4,897,214 B |

后续场景切换：`cp helloworld_<ver> ./example/helloworld && ./start.sh`，无需重编。

## 4. 测试场景 & 原始数据

### 4.1 测试矩阵

沿用 `runtime-fix-execution-log.md` §12.6 三场景：

| 场景 | wrk 参数 | 时长 | 用途 |
|---|---|---|---|
| T1 warmup | `-t2 -c10 --latency` | 5s | 链路探活 + 缓存预热 |
| T2 baseline | `-t4 -c100 --latency` | 30s | 中负载吞吐 |
| T3 high-conc | `-t8 -c500 --latency` | 30s | 高并发长尾 |

### 4.2 13.0 baseline 原始数据

| 场景 | Requests/sec | Avg Lat | p50 | p75 | p90 | p99 | Total req | Transfer/s |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| T1 | **24,413.98** | 405.78us | 392us | 457us | 501us | **600us** | 124,506 / 5.10s | 15.11 MB/s |
| T2 | **220,690.99** | 520.39us | 536us | 591us | 643us | **811us** | 6,622,780 / 30.01s | 136.59 MB/s |
| T3 | **239,554.56** | 2.14ms | 2.19ms | 2.24ms | 2.34ms | **4.21ms** | 7,193,126 / 30.03s | 148.27 MB/s |

原始 wrk 输出：`/tmp/13baseline-bench/T{1,2,3}.txt`

### 4.3 15.0 runtime-fix-done 原始数据

| 场景 | Requests/sec | Avg Lat | p50 | p75 | p90 | p99 | Total req | Transfer/s |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| T1 | **23,757.29** | 425.30us | 399us | 464us | 513us | **838us** | 121,146 / 5.10s | 14.70 MB/s |
| T2 | **203,932.53** | 530.22us | 530us | 585us | 642us | **827us** | 6,120,893 / 30.01s | 126.22 MB/s |
| T3 | **217,099.82** | 2.27ms | 2.24ms | 2.33ms | 2.56ms | **5.38ms** | 6,525,758 / 30.06s | 134.37 MB/s |

原始 wrk 输出：`/tmp/15rfix-bench/T{1,2,3}.txt`

## 5. 同时序对比与 §12.6 历史交叉对比

### 5.1 同时序 A/B（13.0 vs 15.0，同硬件同分钟）

| 场景 | 13.0 req/s | 15.0 req/s | Δ req/s | 13.0 p99 | 15.0 p99 | Δ p99 |
|---|---:|---:|---:|---:|---:|---:|
| T1 t2c10 5s | 24,414 | 23,757 | **−2.69%** | 600us | 838us | **+39.7%** |
| T2 t4c100 30s | **220,691** | **203,933** | **−7.59%** | 811us | 827us | +2.0% |
| T3 t8c500 30s | **239,555** | **217,100** | **−9.37%** | 4.21ms | 5.38ms | **+27.8%** |

### 5.2 §12.6 历史 15.0 (06-02) vs 本次同时序 15.0 (06-03)

| 场景 | §12.6 历史 (06-02) | 本次 15.0 (06-03) | 偏差 |
|---|---:|---:|---:|
| T2 t4c100 30s | 226,065 | 203,933 | **−9.79%** |
| T3 t8c500 30s | 231,106 | 217,100 | **−6.06%** |

**解读**：跨日 6-10% 偏差源于网络/CPU 频率抖动；§12.6 数据**不能**作为干净基线对照，**本次同时序对比才是公允 A/B**。

## 6. 关键发现

### 6.1 性能 regression 实证

**runtime-fix-done (15.0) 相比 13.0 baseline 在同硬件同配置下：**

- T2 中负载 **−7.59%** (220,691 → 203,933 req/s)
- T3 高并发 **−9.37%** (239,555 → 217,100 req/s)，p99 **+27.8%** (4.21ms → 5.38ms)
- T1 轻负载 **−2.69%**，但 p99 长尾 **+39.7%** (600us → 838us)

### 6.2 性能特征推断

| 观察 | 推断 |
|---|---|
| T1 轻负载差距小（仅吞吐 −2.7%）但 p99 长尾恶化 +40% | 单 connection 路径无新瓶颈，但偶发长尾事件（GC/锁/中断）增加 |
| T2/T3 中高并发吞吐与 p99 同步劣化，且高并发劣化更大 | 锁竞争/per-CPU 数据结构改动相关性高 |
| Avg latency 在三场景都增加 ~5% | base 路径开销系统性提升（可能 epoch/atomic 改动） |

### 6.3 怀疑根因（需 perf 进一步证实）

5 个 runtime-fix 修复 + 1 防御性 panic stub 中：

1. **P3 (kern_descrip 边界下移 + `badfileops` 恢复)** —— 涉及 `kern_accept` 错误路径，**accept(2) 是高并发 fastpath**，回退 stub 后 file descriptor 创建/销毁可能引入额外原子操作
2. **P1-2 (atomic.h `__storeload_barrier` `_KERNEL` 路径修复)** —— 修复了 SIGSEGV 但同时**启用了真正的 storeload 屏障**，在 SMP 环境每次 store-load 都有内存屏障开销，对 hot path 影响大
3. **P2 (Phase 2 route_rtentry / 11 stub)** —— 启用了真实路由查找，每包 IP 处理多一次 routing table lookup
4. **Phase 1+2+3 累计 fixes** 把"短路 stub"逐步还原为"真实 FreeBSD 路径"，性能 trade-off 是预期的（**正确性换性能**）

## 7. 后续行动建议（不在本次范围）

| 选项 | 工作量 | 预期收益 |
|---|---|---|
| **A. perf flamegraph 双版叠图** | 0.5d | 直接定位 hot path 分歧（首选） |
| **B. lockstat / mutex 统计** | 1d | 量化锁竞争（若 perf 显示锁热） |
| **C. 二分回退某 commit 单测** | 2d | 隔离 9% gap 来源到具体修复 |
| **D. dpdk EAL 参数对齐复测** | 0.5d | 排除 mempool/RING/burst size 干扰 |
| **E. 接受 9% gap，进入 M5 验收** | 0d | 客观 trade-off：正确性优先（5 个 P0 SIGSEGV 修复价值远大于 9% 吞吐） |

## 8. 系统终态

| 项 | 状态 |
|---|---|
| 13.0 baseline helloworld | 已 kill (PID 1735251)，hugepages 23 rtemap 已清入 trash `20260603-071317-1737799` |
| 15.0 runtime-fix-done helloworld | **仍后台在跑**（PID 1738072, lcore=4, hugepages 23/4096, port0 192.168.1.1:80） |
| 当前 helloworld log | `/tmp/15rfix-bench/hello.log` |
| 双 binary 备份 | 见 §3.3，sha256 已记录 |
| 13.0 baseline 树状态 | `/data/workspace/f-stack-13.0-baseline/lib/` 完整编译，可随时切换 |
| 15.0 主树状态 | `/data/workspace/f-stack/` 不变（runtime-fix-done 状态 + 已 push） |

## 9. 强制规约遵守证据

| 规约 | 证据 |
|---|---|
| DP-10 / `rm_tmp_file.sh` | 全程零直接 rm；3 次脚本调用：①rtemap×23（pre-launch）②lib 产物×195（pre-build）③rtemap×23（pre-15.0-launch） |
| `kill_process.sh` | 全程零直接 kill；2 次脚本调用：①13.0 helloworld 收尾（PID 1071620 in Step 1）②13.0 baseline 跑完后切换 (PID 1735251) |
| `chmod_modify.sh` | 本次未涉及权限修改 |
| commit message 英文 | 见伴随 commit `docs(bench): add 13.0-baseline vs 15.0-runtime-fix-done CVM comparison` |

## 10. 参考

- `runtime-fix-execution-log.md` §12.6 (历史 15.0 单边数据起源)
- `runtime-fix-execution-log.md` §12.10 (同摘要双份落档)
- `runtime-fix-execution-log.md` §12.11 (Phase 4 perf flamegraph 双版叠图)
- spec 06-test-and-acceptance-spec.md §5 (性能基线 NFR-1 整体框架)
- preflight log: `/tmp/13baseline-bench/preflight.log`
- 编译 log: `/tmp/13baseline-bench/lib_make.log` + `/tmp/13baseline-bench/example_make.log`

---

## 11. Phase 4 perf flamegraph 根因定位（追加 — 2026-06-03 15:25-15:33）

### 11.1 工具与采样

| 项 | 值 |
|---|---|
| perf | 6.6.119-49.23.tl4（dnf 装） |
| FlameGraph | `/opt/FlameGraph` (Brendan Gregg, git clone) |
| 采样命令 | `perf record -e cpu-clock -F 999 -C 4 -g --call-graph fp -- sleep 30` |
| 采样事件 | `cpu-clock`（software，因 CVM 虚拟化下 `cycles:P` PMU 不可用，hypervisor 屏蔽） |
| call-graph | `fp`（frame pointer，`-O0 -g` 编译保留），不用 dwarf 避免 16KB stack 干扰 |
| 同步触发 | perf record 后台 + ssh wrk T3 t8c500 30s |

### 11.2 双版采样数据

| 版本 | wrk req/s（perf 干扰下） | swapper idle | sample | folded 行 | svg |
|---|---:|---:|---:|---:|---|
| 15.0 runtime-fix-done | 237,707 | 3.29% | 29,974 | 583 | `/tmp/perf-15rfix/15rfix.svg` |
| 13.0 baseline (after warmup) | 225,777 | 2.82% | 29,974 | 570 | `/tmp/perf-13baseline/13baseline.svg` |

### 11.3 Top 函数 cpu-clock 占比对比

| 函数 | 15.0 % | 13.0 % | Δ | 解读 |
|---|---:|---:|---:|---|
| `tcp_default_output` (15) / `tcp_output` (13) | 4.43 | 3.49 | **+0.94** | TCP stacks vtable wrapper |
| `uma_zfree_arg` | 2.87 | 2.37 | **+0.50** | UMA 释放路径变重 |
| `virtio_recv_mergeable_pkts` | 2.20 | 1.46 | **+0.74** | 收包路径增长 |
| `in_cksum_skip` | 5.01 | 4.94 | +0.07 | 校验和等价 |
| `tcp_do_segment` | 1.39 | 1.73 | -0.34 | 部分逻辑切到 default_output |
| `m_copydata` | 1.53 | 1.64 | -0.11 | mbuf 拷贝接近 |

### 11.4 差分火焰图增长 / 减少 top（按 sample-weight）

**▲ 15.0 grew (regression):**

| 函数（部分调用栈） | Δ |
|---|---:|
| `tcp_default_output` (3 paths) | +99M ×2 + 89M |
| `tcp_usr_send` | +99M |
| `ether_nh_input` | +88M |
| `cubic_ack_received` | +86M |
| `sbcut_locked` | +84M |
| `sbappendstream` | +73M |
| `assert_rw` (rwlock primitive) | +72M |

**▼ 15.0 shrunk:**

| 函数 | Δ |
|---|---:|
| `tcp_output` (旧直接) | **-838M** |
| `tcp_do_segment` | -428M |
| `ip_output` (旧路径) | -173M |
| `callout_reset_tick_on` | -142M |
| `kqueue_kevent` (so_rdknl_lock) | -131M |

### 11.5 根因结论 — 9% gap **不是** runtime-fix 引入

| 真凶（FreeBSD 13→15 vendor 演进） | 占比 | 来源 |
|---|---|---|
| TCP stacks 框架 vtable 派发（tcp_output → tcp_default_output） | 主因 ~+1% | RACK/BBR alternative stacks 抽象层 |
| TCP CUBIC 拥塞控制升级 | ~+0.6% | cubic_ack_received 状态机扩展 |
| socket buffer locking 重构 | ~+1.5% | sbcut_locked / sbappendstream / assert_rw |
| 以太网 mbuf 入口流水线调整 | ~+0.7% | ether_nh_input |
| UMA 释放路径变化 | ~+0.5% | uma_zfree_arg |

**runtime-fix 的 5 个 P0 修复 + 1 防御性 panic stub 全部位于 init / SIGSEGV 路径**，差分图中**完全没有出现** `kern_accept` / `badfileops` / `route_rtentry` / atomic.h 改动相关函数 —— 即 fastpath 不走这些代码。

### 11.6 副发现：log I/O 测试环境噪声 ~5%

两版均有 `__GI___libc_write` → `vfs_write` → `ext4_buffered_write_iter` → `fsnotify` → `inotify_handle_inode_event` 调用链 ≈ 5% CPU。

**这是 helloworld stdout 重定向到 ext4 + 文件被 inotify/fanotify watch 的副作用，不属于 F-Stack 性能问题**。生产环境关闭 stdout log（或重定向 tmpfs / `/dev/null`）可立即回收 ~5%。

### 11.7 行动建议（性价比排序）

| 优先级 | 动作 | 预期收益 | 风险 |
|---|---|---|---|
| ★★★ | 接受 9% gap 进入 M5（正确性 >> 性能） | — | 无 |
| ★★ | helloworld stdout log → tmpfs / /dev/null | +~5% | 无（仅日志输出方式） |
| ★ | `net.inet.tcp.functions_default=freebsd` 关 RACK/BBR 包装层 | +~1% | 失去 RACK 改进 |
| ★ | 调 socket buffer / cwnd 默认 | 待测 | 影响 TCP 行为 |
| × | 回退或二分 runtime-fix commit | — | 重触发 5 个 P0 SIGSEGV |

### 11.8 工具产出归档

| 产物 | 路径 | 大小 |
|---|---|---:|
| 15.0 perf.data | `/tmp/perf-15rfix/perf.data` | 3.76 MB |
| 15.0 svg | `/tmp/perf-15rfix/15rfix.svg` | 146 KB |
| 13.0 perf.data | `/tmp/perf-13baseline/perf.data` | 3.69 MB |
| 13.0 svg | `/tmp/perf-13baseline/13baseline.svg` | 145 KB |
| 差分 svg | `/tmp/perf-diff/diff.svg` | 152 KB |
| 折叠数据 | `*.folded`（各目录） | — |

### 11.9 Phase 4 系统终态

- **13.0 baseline helloworld 仍在跑**（PID 1748870, lcore=4），可立即用于后续验证；切回 15.0 走标准流程：`kill_process.sh + rm_tmp_file.sh /dev/hugepages/rtemap_* + 主树 ./start.sh`
- Phase 4 全程零直接 `rm`/`kill`/`chmod`：`kill_process.sh` × 1、`rm_tmp_file.sh` × 1
- 系统级永久新增：`/usr/bin/perf` (perf-6.6.119-49.23.tl4) + `/opt/FlameGraph/`

---

## §12 helloworld_epoll 验证（范围说明）

### 12.1 范围决策（仅 15.0 单边轻量，未做 13.0 对比）
- 用户决策 Q1=C：仅 15.0 single-side smoke + wrk T2 baseline
- 因此本 baseline 报告**不包含** 13.0 baseline epoll 数据
- 完整 15.0 epoll 数据见 `runtime-fix-execution-log.md §12.12`

### 12.2 工程理由
- helloworld_epoll 的 backend 与 helloworld 完全相同（同 fstack lib、同 lcore=4、同 port 80），仅事件接口从 kqueue 切到 epoll-on-kqueue wrapper
- 13.0 baseline epoll 路径 vs 15.0 epoll 路径的代码差异 = 13.0/15.0 fstack lib 的 epoll wrapper 文件（`src/epoll/`）差异 + FreeBSD 内核 epoll 兼容层差异；两者均已通过 §12.10/§12.11 的 helloworld（kqueue）A/B 间接覆盖
- 单独跑 13.0 baseline epoll 的边际信息量低，被用户判定为不必要的工作量

---

## §13 nginx 双树 A/B 验证 — 13.0 baseline 视角

### 13.1 测试范围
完整 A/B（用户决策 Q2=B）：双树 build → wrk T1/T2/T3 同时间窗，不做 perf flamegraph。15.0 数据 + 完整对比表见 `runtime-fix-execution-log.md §12.13`。

### 13.2 13.0 baseline nginx 构建产物
- 源码：`/data/workspace/f-stack-13.0-baseline/app/nginx-1.28.0/`
- configure：`FF_PATH=/data/workspace/f-stack-13.0-baseline ./configure --prefix=/usr/local/nginx_fstack_13baseline --with-ff_module`
- make 用时：3.40 s
- binary：`/usr/local/nginx_fstack_13baseline/sbin/nginx`，31,695,576 B
- worker_processes 1，listen 80，access_log /dev/null（默认）
- f-stack.conf：`/data/workspace/f-stack-13.0-baseline/config.ini` 的 cp -p 副本（MD5 `9e443c8c494167d9a814a4fb26347869`，与 15.0 副本一致）

### 13.3 启动与健壮性
| 检查项 | 结果 |
|---|---|
| DPDK init 用时 | ≤ 12 s |
| Port 0 Link | Up（virtio_net 0000:00:09.0） |
| f-stack-0 注册 | `Successed to register dpdk interface` |
| Smoke `curl /` | HTTP 200, RTT 1.25 ms, server=192.168.1.1 |
| HTML payload | F-Stack welcome 完整 |

### 13.4 13.0 baseline wrk 完整数据

| 场景 | t/c/dur | Req/sec | Lat avg | p50 | p75 | p90 | p99 | Max | 总请求 | Throughput |
|---|---|---|---|---|---|---|---|---|---|---|
| T1 warmup | 2/10/5s | **26,193.87** | 381.57 µs | 373 µs | 391 µs | 431 µs | 804 µs | 6.50 ms | 133,583 | 19.11 MB/s |
| T2 baseline | 4/100/30s | **189,221.86** | 508.09 µs | 501 µs | 542 µs | 585 µs | 729 µs | 6.96 ms | 5,679,136 | 138.05 MB/s |
| T3 high-conc | 8/500/30s | **229,857.17** | 2.11 ms | 2.09 ms | 2.17 ms | 2.29 ms | 4.47 ms | 30.80 ms | 6,907,693 | 167.69 MB/s |

### 13.5 与 15.0 runtime-fix-done 简比（向 baseline 视角）

| 场景 | 13.0 → 15.0 req/s 变化 | 13.0 → 15.0 p99 变化 | 解读 |
|---|---|---|---|
| T1 | +1.05% | **−37.6%** (15.0 优) | 低并发尾延迟 15.0 显著改善 |
| T2 | −1.05% | +2.5% | 持平 |
| T3 | −0.55% | +18.6% | 高并发尾部 15.0 略恶化但远轻于 helloworld |

13.0 baseline 在 nginx 工作负载下的"参考线"作用：吞吐 / p99 高度稳定，作为后续 15.0 任何回归判定的零点是可靠的。

### 13.6 工件
- `/tmp/13baseline-nginx-bench/{smoke.txt,T1.txt,T2.txt,T3.txt,nginx_stdout.log}` (IP 混淆)
- `/usr/local/nginx_fstack_13baseline/logs/error.log`（fstack 启动日志保留）

### 13.7 系统终态
- 13.0 baseline nginx 已优雅 stop（kill_process.sh）；安装目录 `/usr/local/nginx_fstack_13baseline/` 保留，可重复启用
- 全程零直接 `rm`/`kill`/`chmod`：kill_process.sh × 1（13.0 nginx stop）+ rm_tmp_file.sh × 1（rtemap × 23）


---

## §14 redis 双树 A/B 验证 — 13.0 baseline 视角

### 14.1 测试范围
完整 A/B（与 §13 nginx 同方法论，工具切换为 redis-benchmark）：双树 build → smoke + 三档 redis-benchmark 同时间窗。15.0 数据 + 完整对比表见 `runtime-fix-execution-log.md §12.14`。

### 14.2 13.0 baseline redis 构建产物
- 源码：`/data/workspace/f-stack-13.0-baseline/app/redis-6.2.6/`（fstack 自带 patch，含 `src/ae_ff_kqueue.c` / `src/anet_ff.c`）
- jemalloc autogen 后随 redis make 自动 build（依赖 deps/jemalloc/lib/libjemalloc.a）
- make 时间：30.28 s
- redis-server: `/usr/local/redis_fstack_13baseline/redis-server`，37,174,400 B（链接 fstack lib whole-archive）
- redis-cli: 32,638,584 B（同样链 fstack，仅 server 端可用）
- redis-benchmark: 32,479,000 B（同上；client 端用 stock redis-7.2.7）

### 14.3 启动与健壮性
| 检查项 | 结果 |
|---|---|
| DPDK init 用时 | ≤ 12 s |
| Port 0 Link | Up（virtio_net 0000:00:09.0） |
| f-stack-0 注册 | `Successed to register dpdk interface` |
| Redis init 日志 | `Server initialized` / `Ready to accept connections` |
| Smoke `PING` | PONG |
| Smoke `SET`+`GET` | 写入读出值一致 (`hello-fstack-13.0`) |
| `INFO server` | redis_version=6.2.6, tcp_port=6379, mode=standalone |

### 14.4 13.0 baseline redis-benchmark 完整数据

**T1（c10 n50000 t=ping,set,get --threads 2）**
| 命令 | rps | p50 |
|---|---|---|
| PING_INLINE | 19,968.05 | 0.503 ms |
| PING_MBULK | 19,968.05 | 0.503 ms |
| SET | 19,984.01 | 0.503 ms |
| GET | 19,968.05 | 0.503 ms |

**T2（c50 n500000 t=ping,set,get --threads 4）**
| 命令 | rps | p50 |
|---|---|---|
| PING_INLINE | 99,960.02 | 0.455 ms |
| PING_MBULK | 105,108.27 | 0.447 ms |
| SET | 99,880.14 | 0.455 ms |
| GET | 99,860.20 | 0.455 ms |

**T3（c200 n1000000 t=set,get --threads 8 -P 16）**
| 命令 | rps | p50 |
|---|---|---|
| SET | 1,329,787.25 | 2.175 ms |
| GET | 1,329,787.25 | 1.735 ms |

### 14.5 与 15.0 runtime-fix-done 简比（13.0 视角）

| 场景 | 命令 | 13.0 → 15.0 rps Δ | p50 Δ | 解读 |
|---|---|---|---|---|
| T1 | 全部 | 0.00% | −1.6% ~ −4.8% | client-bound，p50 微幅改善 |
| T2 | PING_INLINE | +5.15% | −1.8% | 15.0 真实领先 |
| T2 | GET | +5.37% | −1.8% | 15.0 真实领先 |
| T2 | SET / PING_MBULK | ≤ +0.10% | 0% | 持平 |
| T3 | SET (P=16) | 0.00% | +2.9% | client-bound throughput，p50 略恶化 |
| T3 | GET (P=16) | +0.13% | +7.8% | 同上 |

13.0 baseline redis 数据高度稳定，作为后续 15.0 性能基准的"零点"参考可靠。

### 14.6 工件
- `/tmp/13baseline-redis-bench/{smoke.txt,T1.txt,T2.txt,T3.txt,redis_stdout.log}`（IP 混淆）
- `/usr/local/redis_fstack_13baseline/{redis-server,redis.conf,f-stack.conf,logs/redis.log}`（保留以备复查）

### 14.7 系统终态
- 13.0 baseline redis 已优雅 stop（kill_process.sh）；安装目录保留可重启
- 全程零直接 `rm`/`kill`/`chmod`：kill_process.sh × 1（13.0 redis stop）+ rm_tmp_file.sh × 1（rtemap × 23）

---

## §15 物理机基线交叉对照（追加 — 2026-06-05；iWiki 4021545579）

### 15.1 数据来源与读法

外部 OSPF/CMC 项目组在 **Intel Xeon 8255C @ 2.5 GHz + Mellanox CX-5 100G + TencentOS 4.4 + Linux 6.6.98 物理机** 上对 freebsd-13.0 / freebsd-15.0 各跑了 helloworld 单核长连接 + nginx_fstack 短连接/长连接 (1/2/4 lcores) wrk 对照。完整二次整理见 `physical-machine-bench-report.md`，本节仅做与 CVM 同时序 A/B 的核心交叉。

### 15.2 helloworld 单核长连接：物理机 vs CVM

| 平台 | 13.0 (req/s) | 15.0 (req/s) | Δ | p99 13.0 | p99 15.0 | Δ p99 |
|---|---:|---:|---:|---:|---:|---:|
| **物理机** (iWiki 4021545579) | 958,109 | 1,056,178 | **+10.24%** | 204 us | 197 us | -3.43% |
| **CVM T2** t4c100 30s（本报告 §5.1） | 220,691 | 203,933 | **−7.59%** | 811 us | 827 us | +2.0% |
| **CVM T3** t8c500 30s（本报告 §5.1） | 239,555 | 217,100 | **−9.37%** | 4.21 ms | 5.38 ms | +27.8% |

**绝对值阶梯**：物理机 helloworld 单核约为 CVM 的 **4.34×**（958k vs 220k），符合 100G mlx5 物理 NIC vs virtio_user 虚拟 NIC 的吞吐阶梯。

**Δ 方向反差**：物理机 **+10.24%** vs CVM **−7%~−9%** 方向相反——这是本项目最具价值的跨平台数据点，**实证 §11.5 perf 根因结论**。

### 15.3 与本报告 §11.5 perf 根因的呼应

§11.5 已通过 perf flamegraph 把 CVM 9% gap 归因为：

| §11.5 真凶（FreeBSD 13→15 vendor 演进） | 物理机端表现 | 验证 |
|---|---|---|
| TCP stacks 框架 vtable 派发（tcp_output → tcp_default_output，~+1pp） | 在物理机 helloworld 同样存在，但被 RACK / CUBIC 收益反超 | ✓ 印证 |
| CUBIC 状态机扩展（~+0.6pp） | 物理机 helloworld p50 -11.57% / Avg -9.66% 直接受益 | ✓ 印证 |
| socket buffer locking 重构（~+1.5pp） | 物理机 helloworld 长连接 +10%；nginx 长连接 1/2/4 核 +5% 系统性净收益 | ✓ 印证 |
| `virtio_recv_mergeable_pkts` (+0.74pp) | 物理机走 mlx5 PMD，**该路径不存在** | ✓ 反证（解释方向反差） |

→ **CVM 9% gap 不是 runtime-fix 引入**这一结论从"perf 单边证据"升级为"perf + 跨平台双重证据"。

### 15.4 nginx_fstack（物理机独占数据，本报告 §13 仅做 CVM 单点 A/B 已 push commit）

物理机 nginx_fstack（外部团队给出）：

| 模式 | 1 核 Δ | 2 核 Δ | 4 核 Δ | 多核 scaling 健康度 |
|---|---:|---:|---:|---|
| 长连接 | +5.06% | +4.76% | +4.83% | 13.0 / 15.0 同 1→4 ≥ 97.5% 效率 |
| 短连接 | -2.25% | -3.65% | **-6.10%** | 13.0 / 15.0 1→4 = 79.6% / 76.5%（共享 listen socket 锁竞争） |

**4 核短连接 -6.10%** 越 NFR-1 阈值（5%）1.10pp，唯一系统性 regression 信号。处置详见 `physical-machine-bench-report.md` §6.2（首选：备案为 trade-off；可选：物理机 perf 双版叠图定位 sonewconn / accept / kern_descrip 路径）。

### 15.5 项目级总判定（含双重基线后）

| 验收维度 | NFR-1 阈值 | 物理机实测 | CVM 同时序实测 | 结论 |
|---|---|---|---|---|
| 单流 TCP 吞吐 | 不退化 > 5% | helloworld +10.24% | helloworld -7%~-9%（perf 归因为 vendor + virtio 而非 runtime-fix） | **PASS**（物理机净收益；CVM 备案为虚拟化路径放大，已 perf 实证） |
| 短连接 QPS | 不退化 > 5% | 1 核 -2.25% / 4 核 -6.10% | 未测 | **观察**（4 核越线 1.10pp，进 99 报告 trade-off 章节） |
| 长连接 QPS（参考） | 信息项 | nginx 1/2/4 核 +4.76%~+5.06% 系统性净收益 | 未测 | ✓ 净收益 |
| RACK 默认化收益（spec 06 §5.3） | 信息项 | helloworld p50 -11.57% / Avg -9.66% / nginx 长连接 +5% 系统性 | 未测 | ✓ 实证 |

### 15.6 本节对项目结论的影响

1. **不更动**本报告 §6.1 / §6.3 的"CVM 9% gap"叙事——CVM 数据本身真实，且与物理机数据互补（一边给出 vendor 收益的 ceiling，一边给出虚拟化路径的 floor）。
2. **强化**本报告 §11.5 的 perf 根因结论——从 1 个证据来源升级为 2 个证据来源（perf flamegraph + 跨平台 Δ 方向反差）。
3. **新增**唯一系统性 regression 信号：物理机 nginx 4 核短连接 -6.10%，处置进 `physical-machine-bench-report.md` §6.2。

### 15.7 数据资产指针

| 项 | 路径 |
|---|---|
| iWiki 原始页面 | `https://iwiki.woa.com/p/4021545579` |
| 二次整理报告 | `physical-machine-bench-report.md`（本目录同级） |
| 06-spec 物理机摘要段 | `06-test-and-acceptance-spec.md` §5.4 |
| IP 混淆约定 | iWiki 中实际服务器 IP（腾讯内网 A/B/C 段）→ 本文统一替换为 `x.x.x.39`（D 段保留以供回溯，A/B/C 段混淆） |
