diff --git a/lib/Makefile b/lib/Makefile
index b06a42fb9..d971678ef 100644
--- a/lib/Makefile
+++ b/lib/Makefile
@@ -218,6 +218,9 @@ endif
 
 CFLAGS+= -DFSTACK
 
+# _M17_PROBE_A: temporary build probe, to be reverted
+CFLAGS+= -DSMP
+
 ifdef FF_ZC_SEND
 CFLAGS+= -DFSTACK_ZC_SEND
 endif
diff --git a/lib/ff_glue.c b/lib/ff_glue.c
index 55fc5c4de..a9bd851c0 100644
--- a/lib/ff_glue.c
+++ b/lib/ff_glue.c
@@ -158,6 +158,15 @@ SYSCTL_PROC(_kern_smp, OID_AUTO, active, CTLFLAG_RD | CTLTYPE_INT, NULL, 0,
     sysctl_kern_smp_active, "I", "Indicates system is running in SMP mode");
 
 int smp_disabled = 0;    /* has smp been disabled? */
+
+/* _M17_PROBE_A: temporary build probe, to be reverted */
+#ifdef SMP
+struct cpu_group *
+smp_topo(void)
+{
+    return (NULL);
+}
+#endif
 SYSCTL_INT(_kern_smp, OID_AUTO, disabled, CTLFLAG_RDTUN|CTLFLAG_CAPRD,
     &smp_disabled, 0, "SMP has been disabled from the loader");
 
