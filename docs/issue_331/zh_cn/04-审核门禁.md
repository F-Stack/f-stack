# Issue #331 审核门禁

## 1. 代码修改审核

### 1.1 kern_event.c kqtimer_sched_callout

- [x] `freebsd/kern/kern_event.c:787-797`：改传相对 ticks（sbinuptime 同尺度计算）
- [x] `ticks` 可见（sys/kernel.h:76 extern，kern_event.c:42 include）✓ M1 子 agent 确认
- [x] `tick_sbt` 可见（sys/time.h:507，经 sys/param.h:144 间接 include）✓ M1 子 agent 确认
- [x] `callout_reset_tick_on` 签名匹配（ff_kern_timeout.c:720）✓ M1 子 agent 确认
- [x] `filt_timerexpire` 前向声明在 L172（先于 L787）✓ M1 子 agent 确认
- [x] 兼容 NOTE_ABSTIME（kc->next=to 绝对，已过期则 to_ticks=0）
- [x] filt_timerexpire_l 周期重排路径正确（kc->next 在 L843/L845 推进后才 L872 reschedule）
- [x] 无 lint 错误
- [x] 无 debug printf 残留

### 1.2 ff_kern_timeout.c callout_tick

- [x] `lib/ff_kern_timeout.c:355-357`：移除 #ifndef FSTACK guard，调用 softclock(cc)
- [x] `lib/ff_kern_timeout.c:199`：添加 `static void softclock(void *arg);` 前向声明
- [x] softclock() 无 SWI/scheduler 依赖，用户态安全 ✓
- [x] 无 lint 错误

### 1.3 blast radius

- [x] 仅 kern_event.c EVFILT_TIMER 手动算绝对 sbt（broken），其他 C_ABSOLUTE 调用方（tcp_timer/subr_sleepqueue/tcp_hpts/iflib）经 callout_when stub 保持相对 → 安全 ✓ M1 子 agent 确认
- [x] softclock 改动是叠加（普通 callout 轮启动），不影响 HPTS 路径（TCP 仍由 HPTS 驱动）

## 2. 测试审核

- [x] 复现测试：bug 状态 90s 零触发（延迟 ≈ uptime 35 天）✓
- [x] T1 精度：修复后 30 次触发，间隔 1.000s ✓
- [x] T2 周期重设：rearm 后 ~1s（20ms 漂移自纠正）✓
- [x] T3 TCP 回归：echo 正常 ✓
- [x] T4 默认配置：T3 覆盖 ✓

## 3. 文档审核

- [x] plan.md：完整方案 ✓
- [x] 00-调研总览：根因 + 修复 + 测试结果 ✓
- [x] 01-测试方案与环境：环境 + 矩阵 ✓
- [x] 02-测试结果与数据分析：复现 + T1-T4 数据 ✓
- [x] 03-根因分析与修复方案：双重 bug + 方案 + 排除 ✓
- [x] 04-审核门禁：本文件 ✓
- [x] issue-ana.md 中英文 #331/#701/#702 条目更新 ✓

## 4. 规约合规

- [x] rm→rm_tmp_file.sh，kill→kill_process.sh，chmod→chmod_modify.sh
- [x] lib 最小注释（kqtimer_sched_callout 加 2 行必要注释说明为何绕过宏）
- [x] commit message 英文 1-3 句
- [x] config.ini 本地测试值不入库
- [x] 改代码先 make clean 再编译（每次均 clean build）
- [x] 写代码/文档 与 审核不同 agent（主 agent 写文档，子 agent #2 审核）

## 5. 潜在风险

- **native-mt 多线程**：softclock 在每 worker 的 ff_hardclock_worker 上下文运行，处理本 worker callout 轮。单线程已验证，多线程建议后续压测。
- **softclock 性能**：每 10ms 检查 callout 轮，need_softclock=false 时 O(1)。大量 callout 时 softclock 遍历桶，但 F-Stack 场景 callout 数量有限。
- **NOTE_ABSTIME**：修复后兼容（已过期立即触发，未来按相对 ticks 调度），但精度受 tick 粒度限制。
