# 07-后续编码工作分解

> 修订记录：2026-08-07 据门禁报告 R1/R6/R8 修正 C28 行号（2081→2080、2056→2055）并补文本锚点、修正 Makefile 行数引用、统一单测用例数口径。
> 修订记录（2026-08-10）：据 `04` 第五次修订（K4 为首选、K3-corrected 为退备）及 `11` 的 Q1/Q2/Q3 代码坐实 + E4a 决定性实验结果，M3 KNI 部分全面重写：删除 K3 的"多消费者 ring"过度设计（Q1：`kni_rp` 已是 `RING_F_SC_DEQ` 单消费者，保持不动）与"按 `nb_lcores` 折算 ratelimit"过度设计（Q2：`kernel_packets_ratelimit` 在 `kni_process_tx` 中只有 owner 执行，天然单点限速）；新增 K4 方案 7 处代码改动点（据 `_poc_kni_secondary.patch` 已落地验证）；N2 修正提升为 K4 前置必做（C32 → 前置）。PoC 补丁 `_poc_kni_secondary.patch` 的 7 处改动已应用于工作区源码（`ff_kni_is_runtime_owner()` 已存在于 `lib/ff_dpdk_kni.c:100-106`、`owner_proc_id` 已存在于 `lib/ff_config.h:342`）。

## 摘要

本文是 issue #1078「primary 稳定性控制」在**用户决定正式落地 S1「primary-slim」**前提下的编码工作分解。方案已由 `03` 定档、`04` 补齐 KNI/控制面归属、`05` 的 E3 系列 PoC 实机跑通（3 行改动 + 1 项配置，杀 slim primary 后 12/12 连接零中断）、`06` 确认无性能回归。本文把方案拆成 **4 个里程碑、38 个可执行改动点、2 个新增配置项**，逐项给出文件:行号、改法要点、依赖、风险与回滚方式，并明确标注哪些已被 PoC 实测覆盖、哪些是全新未验证代码。

## 关键结论

1. **PoC ≠ 正式实现**。PoC 只做了 2 处（B1 放宽 + B3 池公式近似），用"primary 不在任何 `lcore_list`"**自动推导**而非显式开关；`03` 的改动 #3（跳过 `rte_eal_cleanup()`）、#4（ifp 归属日志）、idle_sleep 下限、全部校验链（V2~V6）、KNI、MTU 归属**均未实现**。正式落地的新增代码量约为 PoC 的 10 倍。
2. **M1（最小可跑集）16 个改动点、4 个文件，是唯一"必须一次做完才能跑"的批次**；其中 C09+C10 必须同批提交（C09 单独上会造成 mbuf 池欠配）。M1 完成即可复现 PoC 的全部实测结论，并额外解决 P2 的 CPU 空转问题。
3. **最强回滚点是配置级**：`primary_slim=0`（默认值）+ 恢复 `lcore_list` 注释 ⇒ 行为与改造前逐字一致。这是选显式开关而非自动推导的直接收益，任何里程碑都可用一行配置回退。
4. **两处必须写进代码注释与运维文档的高危点**：(i) 绝不能给 slim primary attach ifp，否则 `tx_queue_id[port]` 恒为 0 会与 `lcore_list[0]` 那个 secondary 并发写同一条非 MT-safe 的 tx 队列（`03` R3）；(ii) slim primary 是**常驻**控制面进程，禁止优雅退出（`rte_eal_cleanup()` 会抽走全体 secondary 的共享内存，`02` N4）。
5. **KNI（M3）是改动量最大的批次**（K4 首选方案 9 个点含 N2 前置；K3-corrected 退备 7~8 个点）。据 `04` 第五次修订与 `11` 的 Q1/Q2/Q3 代码坐实及 E4a 决定性实验（secondary 通过 `VDEV_SCAN_REQ` 成功发现并 probe primary 创建的 virtio_user 口），**K4（secondary 作运行时 KNI owner）为首选**——不需要新增 `kni_tx_ring`（owner secondary 直接用自己的 tx 队列发出，引自 `11` §3.8），KNI owner 崩溃只影响 KNI 不影响控制面，恢复只需重启该 secondary。K3-corrected（K3 + Q1/Q2 纠正）为退备方案。M1/M2 阶段用配置期显式拒绝 `primary_slim=1 && kni.enable=1` 把它隔离在外。

> **输入基线声明**：本文写作时 `03`/`04` 已包含 2026-08-07 第二轮修订（据 E2e/E10 上调 S1、下调 S5 为兜底、A4 闭环、新增风险 R14）。本文引用的 `03` §5 为 7 步最小集 + 6 步完整集 + 5 项 O 级 + R0~R3 回滚点的修订版。

---

## 一、前提与范围

### 1.1 本文的定位

| 项 | 内容 |
|---|---|
| 触发条件 | **仅当用户决定把 S1 正式落地**时执行；#1078 的调研结论（可行，有条件）本身不依赖本文 |
| 输入 | `00`（诉求 R1~R8/ 判据 C1~C6）、`02`（B1~B8、D.1 十二项、D.2 的 N1~N8、任务 E 的 FP-1/FP-2）、`03`（S1 设计 + §5 顺序建议 + R1~R14）、`04`（K4 首选 / K3-corrected 退备 / 控制面归属 / `msg_ring` 裁决 / `nb_dev_ports` 修正）、`05`（E3 系列 PoC）、`06`（性能基线 + P2）、`11`（Q1/Q2/Q3 代码坐实 + E4a 决定性实验 + K4 方案）、`_poc_kni_secondary.patch`（K4 PoC 补丁已落地） |
| 输出 | 本文（工作分解）+ `08`（测试规格）；**本轮不写任何 lib/ 代码、不写测试代码、不编译、不提交** |
| 前置约束（M1~M2 期） | `thread_mode=0`、`kni.enable=0`、`nb_procs >= 2`（推荐≥3，规避 `03` R11 的单 secondary reta 边界） |

### 1.2 PoC 与正式实现的差异（必须明确，避免"PoC 已通就等于实现完成"的误判）

| 维度 | PoC（`05` §11.1，`_poc_primary_slim.patch`） | 正式实现 |
|---|---|---|
| 开关语义 | **无开关**，由"primary 不在任何 `lcore_list`"自动推导（`rte_eal_process_type() != RTE_PROC_PRIMARY` 加进 `rte_exit` 条件） | 显式 `[dpdk] primary_slim`（`03` §2.1.1裁决候选乙），保住 fail-fast |
| mbuf 池公式 | `nb_ports * nb_lcores`（近似，去掉本进程队列数依赖） | `Σ_{p∈portid_list} port_cfgs[p].nb_lcores`（`03` §2.6 精确式） |
| 校验链 | **完全没有**（V2~V6 全缺） | 6 条校验（含 `kni`/`thread_mode`/`nb_procs` 互斥与自相矛盾检测） |
| 退出语义 | 未实现（primary 常驻不退出 ⇒ `rte_eal_cleanup()` 未被触发，问题被掩盖） | C11 门控跳过 + C12 停机告警 |
| idle_sleep | 未实现 ⇒ **实测占满一核99.8%**（`06` §4.4P2） | C13 引入 `primary_slim_idle_sleep`（默认 1000us） |
| ifp 归属 | 未实现（靠 `nb_tx_port == 0` 天然 no-op，日志缺失） | C14 显式 NOTICE 日志 + C37 断言 |
| KNI | PoC `_poc_kni_secondary.patch` 已落地 7 处改动（`ff_kni_is_runtime_owner()` 等，已复核存在于源码），但 `ff_kni_process` 移出 rx 循环 + N2 修正 + B2 校验调整 + E4b~E4f 实机验证均未做 | M3 按 `04` K4 首选落地（含 N2 前置）/ K3-corrected 退备 |
| 提交状态 | 仅作调研证据留档，**不入库** | 分4 个里程碑入库 |

### 1.3 行号来源约定

- 标注「已复核」= 本文写作时由worklist 直接读取源码确认。
- 标注「引自 02 / 03 / 04」= 沿用上游文档行号，未逐条重读。
- 本文已复核的文件区段：`lib/ff_dpdk_if.c:470-548`、`:2810-2898`；`lib/ff_config.c:1033-1038`、`:1070-1075`、`:1390-1519`；`lib/ff_config.h:291-316`；`config.ini:1/3/6-8/51/189/307`；`tests/unit/*`、`tests/integration/*`（见 `08`）。

---

## 二、里程碑划分

### 2.0 总览

| 里程碑 | 目标 | 改动点数 | 文件数 | 前置 | 可独立编译 | 可独立验证 | 回滚点 |
|---|---|---|---|---|---|---|---|
| **M1** 最小可跑 | slim primary 能启动、不收包、不烧核、不拆EAL；数据面全量正常 | 16（C01~C16） | 4（`ff_config.h`/`ff_config.c`/`ff_dpdk_if.c`/`config.ini`） | 无 | ✅ | ✅（复现 E3/E3b/E3c + E12） | R1 |
| **M2** 控制面完整性 | MTU 归属、RSS 诊断、tools 语义、slim 角色可识别 | 6（C17~C22） | 4（`ff_dpdk_if.c`/`ff_msg.h`/`tools/`/文档） | M1 | ✅ | ✅（工具链用例） | R2 |
| **M3** KNI 归属 | `primary_slim=1 && kni.enable=1` 可用（按 `04` K4 首选 / K3-corrected 退备） | 9（C23~C31，K4 方案含 N2 前置） | 5（`ff_dpdk_if.c`/`ff_dpdk_kni.c`/`ff_dpdk_kni.h`/`ff_config.c`/`ff_config.h`） | M1（M2 可并行） | ✅ | ✅（KNI 双向连通 + 广播入内核 + E4a/E4b 验收） | R3 |
| **M4** 加固与清理 | `nb_dev_ports` 语义、缺失门禁、死代码、只读API、断言 | 7（C32~C38） | 4（`ff_dpdk_if.c`/`ff_api.h`/`doc/`/`config.ini`） | M1 | ✅ | ✅（单测 + 编译组合矩阵） | 各点独立 |

**合计 38 个改动点**。按 `02` D.1 / `03` §5 的分级：**M 级 16**（C01~C16）、**S 级 12**（C17~C20、C23~C30）、**O 级 10**（C21、C22、C31、C32~C38）。其中 C32（N2 修正）在 K4 方案下升级为 M3 前置必做。

> 说明一处级别调整：`02` D.1 把「`thread_mode` 与 slim 互斥校验」列为 **O#11**，而 `03` §2.2 的 V4 把它列为**必做**（理由：`thread_mode` 下 `nb_procs` 被强制为 1，`lib/ff_config.c:1496-1498`，已复核，slim 语义不成立）。本文**采用 `03`**，即 C06 属 M1。

### 2.1 M1：最小可跑（`kni.enable=0` 前提）

| 项 | 内容 |
|---|---|
| **目标** | 解除 B1（`nb_rx_queue == 0` 的 `rte_exit`）+ 修正 B3（mbuf 池公式）+ 落地配置项与全部校验 + slim 精简主循环的两处真实改动（退出门控、idle_sleep 下限） |
| **改动文件** | `lib/ff_config.h`、`lib/ff_config.c`、`lib/ff_dpdk_if.c`、`config.ini`（注释块，不含本地测试值） |
| **验收标准** | ① `make clean && make` 在 `lib/` 与 `example/` 均 exit 0 无 error（规约：改代码必须 clean build）；② 单测 `tests/unit/ make test` 全绿+ 新增 `08` 的 UT-1078-01~13；③ 实机：primary + 2 secondary 启动成功，`lcore_list=1,2` 时网卡只建 2 队列；④ 客户端 12 条连接 12/12；⑤ `kill_process.sh` 杀 slim primary 后 12/12 **不变**（对照 `05` E2 的 11/20 失效）；⑥ slim primary 稳态 CPU ≤ 5%（对照 `06` P2 的 99.8%）；⑦ `primary_slim=0` 时行为与改造前逐字一致 |
| **依赖** | 无 |
| **回滚点** | **R1**：C01~C16 是一个自洽提交串，可整体 revert；未触碰 KNI/MTU/`nb_dev_ports` |
| **不做** | KNI（被 C05 配置期拒绝）、MTU 归属、`nb_dev_ports`、`init_flow` 门禁 |

### 2.2 M2：控制面完整性

| 项 | 内容 |
|---|---|
| **目标** | 处理 slim 引入的**唯一功能回归**（`03` R4 / `02` N5：`ff_ifconfig mtu` 假成功）+ 恢复 `FF_RSS_DIAG` 诊断能力 + 让运维/工具能识别 slim 角色 |
| **改动文件** | `lib/ff_dpdk_if.c`、`lib/ff_msg.h`、`tools/`（可选）、`doc/` |
| **验收标准** | ① P1 档：slim 下 `ff_ifconfig <if> mtu 9000` 返回**可见错误**而非静默成功；非 slim 多进程下返回值与改前一致（零回归）；② P2 档（若做）：MTU 请求经 `msg_ring[0]` 由 primary 实际执行、`rte_eth_dev_set_mtu()` 生效、各secondary `ctx->mtu` 同步；③ `FF_RSS_DIAG` 编译下 slim primary 的 reta 一致性诊断**不再被静默跳过**；④ `ff_top`/`ff_sysctl` 不带 `-p` 不挂起（`04` §4.1 候选甲，代码 0 改动，需用例证明） |
| **依赖** | M1（`primary_slim` 门控变量必须已存在） |
| **回滚点** | **R2**：C17~C22 可独立 revert，回退后 MTU 行为退回"静默假成功"（回归可见性丢失，但不影响数据面） |
| **判断** | P1（C17，2 行）**必做**；P2（C18）视是否有"运行期改 MTU"的实际需求决定，`04` §3.1 已警示P2 的 `-EBUSY` 分支会 `dev_stop/start` ⇒ **瞬断全体 secondary 队列** |

### 2.3 M3：KNI 归属（按 `04` K4 首选 / K3-corrected 退备）

| 项 | 内容 |
|---|---|
| **目标** | 放开 `primary_slim=1 && kni.enable=1`：K4 方案下指定 secondary 作运行时 KNI owner，直接用自己的 tx 队列发出，**不需要新增 ring**；K3-corrected 退备方案下 primary 保留 KNI owner 但完全不碰物理口队列，「内核 → 物理口」方向经新增 `kni_tx_ring`（`RING_F_SP_SC`）借道 secondary |
| **路径选择** | **K4（`04` §2.4 首选，E4a 已实测验证）**。据 `11` §3.8 关键发现——`kni_process_rx` L190 的 `rte_eth_tx_burst(port_id, queue_id, ...)` 中 `queue_id` 来自 owner 自己的 rx 队列——**K4 的 owner secondary 直接用自己的 tx 队列发出，不需要新增 `kni_tx_ring`**。K3-corrected（K3 + Q1/Q2 纠正）为退备方案。K1（保留最小队列）是 S3 的退化形态、只作"KNI 必开且不接受 KNI 改造风险"时的兜底 |
| **改动文件** | `lib/ff_config.h`、`lib/ff_config.c`、`lib/ff_dpdk_kni.h`、`lib/ff_dpdk_kni.c`、`lib/ff_dpdk_if.c` |
| **验收标准** | ① `primary_slim=1 && kni.enable=1` 配置期**通过**（C29 放开 V3）；② KNI 双向连通：内核侧 `ping`/`ssh` 走 KNI 旁路成功、f-stack → 内核方向正常（E4b）；③ **广播/多播（ARP 请求 / IPv6 NS / DHCP）进入内核且恰好一份**（C28 的核心判据：改成仅 `enable_kni` 会导致 N 份重复，务必用例覆盖；E4d）；④ K4 方案下杀 owner secondary 后 KNI 中断但数据面仍 12/12、重启该 secondary 后 KNI 恢复（可原地重启）；K3-corrected 下杀 slim primary 后 KNI 失效但数据面仍 12/12（KNI 与控制面同生共死，属预期）；⑤ KNI 吞吐不低于非 slim 基线的可接受区间（轮询频率从"每 rx 队列一次"降为"每轮一次"）；⑥ E4a 验收项：secondary 通过 `VDEV_SCAN_REQ` 发现 virtio_user 口、独立 probe、访问共享 KNI ring（已实测通过） |
| **依赖** | M1。K4 方案**需要**前置修 N2（owner secondary 需要 `nb_dev_ports` 算 `kni_stat[port_id]->port_id`，`04` §2.4）、**需要**改 `total_nb_ports *= 2` 条件为仅 `enable_kni`（否则 primary 不配置 virtio_user 口）；K3-corrected 不需要前置修 N2（`nb_dev_ports` 仍只在 primary 侧使用）、不需要改 `total_nb_ports` 条件（owner 仍是 primary），这是 K3-corrected 相对 K4 的优势 |
| **回滚点** | **R3**：C23~C31 作为独立提交串，可单独 revert 而不影响 R1/R2；revert 后 C05 的"配置期拒绝 KNI"需一并恢复 |
| **风险** | K4：vdev 可见性依赖 primary 在线（secondary 重启时需 primary 响应 `VDEV_SCAN_REQ`）；改动面较大（含 N2 + `total_nb_ports` 条件 + owner 判定 + secondary `kni_stat` 分配）。K3-corrected：新增一跳 `kni_tx_ring`（延迟 + ring 满丢包）；KNI 与 primary 绑定（primary 崩溃 = KNI 断且不可原地恢复，E10 已证）。两方案共有风险：广播/多播门禁改造后 `console/general_packets_ratelimit` 聚合放大约 N 倍（但 `kernel_packets_ratelimit` 在 `kni_process_tx` 中只有 owner 执行，天然单点限速，引自 `11` §2.5 Q2 纠正，不需折算） |

### 2.4 M4：加固与清理

| 项 | 内容 |
|---|---|
| **目标** | 把 slim 暴露/放大的既有缺陷收口，并为 app 提供角色识别能力 |
| **改动文件** | `lib/ff_dpdk_if.c`、`lib/ff_api.h`、`doc/`、`config.ini` |
| **验收标准** | ① `nb_dev_ports` 在 secondary侧取值正确（C32，源码注释 `/* secondary is not correct */` 可删）；② `FF_FLOW_ISOLATE` / `FF_FDIR` 编译组合下 secondary 不再越权安装 rte_flow 规则、无重复告警（C33）；③ `init_kni()` 变量语义清晰、死代码删除（C34）；④ `ff_is_slim_primary()` 可被 app 调用并返回正确值（C36）；⑤ slim primary 下误调 `ff_veth_attach()` 立即 `rte_exit`（C37，防R3 风险复现）；⑥ 全部编译组合矩阵通过 clean build |
| **依赖** | M1；C32 若 M3 选了 K4 则升级为**前置必做**（owner secondary 需要 `nb_dev_ports` 算 `kni_stat[port_id]->port_id`）；K3-corrected 下可延后 |
| **回滚点** | 各点相互独立，可逐点 revert |

---

## 三、逐项改动清单（可直接执行）

> 列说明：**级别** = `02` D.1 / `03` §5 的 M/S/O；**PoC** = 该改动是否被 `05` E3 系列 PoC 实测覆盖（✅ 已验证 / ◐ 部分/间接 / ❌ 全新未验证）；**风险**低/中/高。

### 3.1 M1：配置项与校验链（C01~C08）

| 编号 | 级别 | 文件:行号 | 类型 | 具体改法要点 | 依赖 | PoC | 风险 | 回滚 |
|---|---|---|---|---|---|---|---|---|
| **C01** | M | `lib/ff_config.h:295-296`（已复核，紧邻 `thread_mode`/`nb_threads`） | 新增 | `struct ff_config.dpdk` 增`int primary_slim;` 与 `unsigned primary_slim_idle_sleep;`。后者建议紧邻 `idle_sleep`（`:311-312`，已复核）以便对读| — | ❌ | 低 | 删字段 |
| **C02** | M | `lib/ff_config.c:1035-1036`（已复核，`MATCH("dpdk","thread_mode")` 旁） | 新增 | 追加 `else if (MATCH("dpdk","primary_slim")) pconfig->dpdk.primary_slim = atoi(value);`；`primary_slim_idle_sleep` 建议插在 `:1072-1073` 的 `MATCH("dpdk","idle_sleep")` 旁（已复核），保持同类项聚集 | C01 | ❌ | 低 | 删分支 |
| **C03** | M | `lib/ff_config.c:1508-1519`（`ff_default_config()`，已复核） | 新增 | 结构体已 `memset`（`:1511`，已复核），`primary_slim` 无需显式置 0，但为可读性显式写 `cfg->dpdk.primary_slim = 0;`；`primary_slim_idle_sleep` **必须显式**置 `1000`（默认 1ms，否则 memset 后为 0 ⇒ 退化为空转烧核） | C01 | ❌ | **中**（漏置默认值会让 C13 失效，是最容易埋雷的一行） | 删两行 |
| **C04** | M | `lib/ff_config.c:1414` 之后（已复核，与 `:1419-1435` 的 KNI 检查并列，同在 per-port 循环内） | 新增 | **V2**：`primary_slim && proc_type=="primary"` 时逐 port 校验 `proc_lcore[proc_id]` **不在** `pc->lcore_list` 中；若在则 `fprintf(stderr, "primary_slim=1 but primary lcore %d is still in port %d's lcore_list\n", ...)` + `return -1`。可直接复用 `:1421-1428` 的遍历写法（镜像逻辑） | C01/C02 | ❌ | 低 | 删代码块 |
| **C05** | M | `lib/ff_config.c:1419-1435`（已复核，B2 的 KNI 检查） | 修改 | 在该块**之前**插入互斥拒绝：`primary_slim && kni.enable` →报错 `primary_slim=1 is incompatible with kni.enable=1 (see issue #1078 M3)` + `return -1`（约 3 行）。**不改动** `:1419-1435` 本体，保证 `primary_slim=0` 时 KNI 校验逐字不变（零回归） | C04 | ❌ | 低 | 删 3 行；M3 的 C29 会重写此处 |
| **C06** | M（`02` 原列O#11） | `lib/ff_config.c:1484`（已复核，`if (cfg->dpdk.thread_mode)` 块）之前 | 新增 | **V4**：`thread_mode && primary_slim` → 报错 `primary_slim=1 is incompatible with thread_mode=1` + `return -1`。必须插在 `:1484` **之前**，因为 `:1496-1498` 会把 `nb_procs` 改成 1（已复核），之后再判 `nb_procs` 就失真 | C01 | ❌ | 低 | 删代码块 |
| **C07** | M | `lib/ff_config.c:1484` 附近（与 C06 同处） | 新增 | **V5**：`primary_slim && nb_procs < 2` → 报错。理由：唯一进程不收包 ⇒ 所有 port 的 `nb_queues` 为 0 ⇒ `rte_eth_dev_configure(port_id, 0, 0, ...)` 语义不成立（`03` V5，行号引自 02） | C01 | ❌ | 低 | 删代码块 |
| **C08** | M | `lib/ff_config.c:1484` 附近（与 C06 同处） | 新增 | **V6**：slim 生效时打一条 INFO（含 `primary_slim`、`proc_id`、`proc_type`、各 port 的 `nb_lcores`）供跨进程配置对账（`03` R13：各进程用独立 `config.ini` 时取值不一致无法在代码层强制）；并对 `primary_slim && nb_procs < 3` 打 WARNING（`03` R11 单 secondary reta 边界）。**不得**因此 return -1 | C01 | ❌ | 低 | 删日志 |

### 3.2 M1：启动路径与主循环（C09~C16）

| 编号 | 级别 | 文件:行号 | 类型 | 具体改法要点 | 依赖 | PoC | 风险 | 回滚 |
|---|---|---|---|---|---|---|---|---|
| **C09** | M | `lib/ff_dpdk_if.c:508-510`（已复核：`if (ff_cur_lcore_conf()->nb_rx_queue == 0) rte_exit(EXIT_FAILURE, "lcore %u has nothing to do\n", lcore_id);`） | 修改 | 改为：若 `ff_global_cfg.dpdk.primary_slim && rte_eal_process_type() == RTE_PROC_PRIMARY` → `ff_log(FF_LOG_NOTICE, FF_LOGTYPE_FSTACK_LIB, "primary_slim: primary holds no rx/tx queue, running control-plane only\n")` 后 `return 0`；否则保持原 `rte_exit`。**这是第一道硬墙**：`init_lcore_conf()` 是 `ff_dpdk_init()` 第一步（`:1682`，引自 02），primary 在此退出 ⇒ 池/ring/msg_ring/port_start 全不执行 ⇒ secondary 的 9 处 `rte_*_lookup`拿 NULL ⇒ 全组起不来（`02` B8） | C01~C08 | ✅（PoC 用 `rte_eal_process_type() != RTE_PROC_PRIMARY` 无条件放宽；正式实现多一层 `primary_slim` 门控） | **中**（误改会放宽 `primary_slim=0` 的 fail-fast，必须由 `08` UT-1078-07/IT-1078-03 守住） | 恢复原 3 行 |
| **C10** | M | `lib/ff_dpdk_if.c:535`（已复核：`uint32_t nb_rx_queue = ff_cur_lcore_conf()->nb_rx_queue * nb_lcores;`） | 修改 | 改为按port 求和、不依赖本进程队列数：`nb_rx_queue = Σ_{j<nb_ports} port_cfgs[portid_list[j]].nb_lcores`。**等值性**：同构配置下 `Q*L = P*N = Σ_p nb_lcores_p` ⇒ 对既有配置**逐字节相同**（`03` §2.3 改动 #2 论证）。**必须与 C09 同批提交**，否则 slim 下 RX 项归零 ⇒ 池对全体 secondary 严重欠配（`02` B3；`03` 例 B：L=8 时少 8192 个 mbuf ≈ 19MB） | C09 | ◐（PoC 用 `nb_ports * nb_lcores` 近似式，正式实现改精确式；两者在同构配置下同值） | **中**（异构 `lcore_list` 下与旧式不同，需 `08` 的 L=2/L=8 等值性单测 + 多 port 用例） | 恢复原 1 行 |
| **C11** | M | `lib/ff_dpdk_if.c:2879-2885`（已复核：`ff_unload_config(); rte_eal_cleanup(); ff_log_close();`） | 修改 | 用 `if (!(ff_global_cfg.dpdk.primary_slim && rte_eal_process_type() == RTE_PROC_PRIMARY))` 包住 `ff_unload_config()` + `rte_eal_cleanup()`，slim primary 只执行 `ff_log_close()`。理由（`02` N4 + `01` §1.2.1/§1.2.2/§1.2.5）：`rte_eal_cleanup()` 释放 hugepage/memseg/多进程通道，而全体 secondary 热路径指针都指向这些共享内存 ⇒ "primary 优雅退出"会变成"全体 secondary 内存被抽走"。E2/E2e 只覆盖**强杀**路径，结论不能外推到优雅退出 | C01 | ❌（PoC 未触发该路径：primary 常驻不退出） | **高**（错误门控会让非 slim 场景漏掉 cleanup ⇒ valgrind/`make check` 会暴露；且该分支实机验证需 E8 用例） | 去掉 `if` 包裹 |
| **C12** | M | `lib/ff_dpdk_if.c:2888-2891`（`ff_dpdk_stop()`，已复核）或 `:2876` 之前 | 新增 | slim primary 收到停机请求时打 `FF_LOG_WARNING`：说明退出后进入控制面降级态（mp_socket 服务端消失 ⇒ IPC /扩堆代理 / 中断能力丧失；本进程 `/dev/uioX` fd 关闭 ⇒ refcnt −1），只要仍有数据面进程存活（refcnt ≥ 1）数据面与secondary 原地重启不受影响（`05` E2e 复现两次），但需尽快按运维流程拉回 primary（注：**primary 不可原地拉回**，`05` E10 实测 `Cannot init memzone`） | C11 | ❌ | 低 | 删日志 |
| **C13** | M | `lib/ff_dpdk_if.c:2822-2828`（已复核：`if (likely(idle && idle_sleep)) rte_delay_us_sleep(idle_sleep);`） | 修改 | slim primary 用 `primary_slim_idle_sleep` 替代 `idle_sleep`（取`max(idle_sleep, primary_slim_idle_sleep)` 或直接替换，建议直接替换并在C08 的 INFO 中打印生效值）。**必要性**：slim 下 `idle` 恒为 1，`config.ini:51` 默认 `idle_sleep=0`（已复核）⇒ **实测占满一核 99.8%**（`06` §4.4 P2）。代价：`msg_ring` 消息与 timer 精度延迟 ≤ 1ms，控制面可接受 | C01/C03 | ❌（`05` E12 待做） | 中（取值过大会让`ff_sysctl`/`ff_top` 响应变慢到可感知，`03` 未决问题 4，需 `08` 量化） | 恢复原逻辑 |
| **C14** | M | `lib/ff_dpdk_if.c:2850-2865`（`ff_dpdk_if_up()`，已复核） | 新增 | **裁决：代码逻辑不改**（`qconf->nb_tx_port == 0` ⇒ 循环 0 次 ⇒ 天然 no-op），只在slim primary 下加一条 NOTICE："slim primary has no f-stack network interface (noifp attached); use `-p <data-plane proc id>` for interface/route commands"。**绝不能给 slim primary attach ifp**：`lcore_conf` 是零初始化全局数组（`:126`，引自 03），slim primary 从未执行 `:501` 的 `tx_queue_id[port_id] = queueid`（已复核）⇒ 该值恒为 **0** ⇒ `send_burst()` 会 `rte_eth_tx_burst(port, 0, ...)`撞进 `lcore_list[0]` 那个 secondary 的 tx 队列，而 DPDK tx 队列非 MT-safe（`03` R3） | C09 | ◐（PoC 实测 primary 日志无 `Successed to register dpdk interface`，`02` N6 得到印证；NOTICE 日志本身未实现） | 低 | 删日志 |
| **C15** | M | `lib/ff_dpdk_if.c:459-460`（`thread_mode` 分支的 `rte_exit`，引自 03） | 修改（仅注释） | **不改逻辑**（slim 与 `thread_mode` 已由 C06 互斥），仅加一行注释说明互斥关系。遵循最小注释规约：一行足够 | C06 | ❌ | 低 | 删注释 |
| **C16** | M | `config.ini`：`[dpdk]` 段（`:1`，已复核），建议插在 `:6-8` 的 `thread_mode` 注释块之后；`idle_sleep` 说明在 `:51`（已复核） | 新增 | 追加两项的注释说明块与**注释掉**的示例（`#primary_slim=0` / `#primary_slim_idle_sleep=1000`），并在 `[port0]` 的 `#lcore_list=0`（`:189`，已复核）附近补一句"`primary_slim=1` 时必须显式配置 `lcore_list` 且排除 primary 的 lcore"。**规约**：`git add` 时只提交本条注释块，**不得**带入`lcore_mask` / `idle_sleep` / `[portN]` 本机 IP 等本地测试值 | C01/C02 | ❌ | 低| 删注释块 |

### 3.3 M2：控制面完整性（C17~C22）

| 编号 | 级别 | 文件:行号 | 类型 | 具体改法要点 | 依赖 | PoC | 风险 | 回滚 |
|---|---|---|---|---|---|---|---|---|
| **C17** | S | `lib/ff_dpdk_if.c:297-298`（`ff_dpdk_if_set_mtu()` 的 primary 门禁，引自 03/04） | 修改 | **MTU P1（2 行）**：`return 0` 改为 `return EPERM`，**但必须用 `ff_global_cfg.dpdk.primary_slim` 门控**，只在 slim 下返回 `EPERM`，保证非 slim 多进程场景`ff_ifconfig` 返回值零回归（`04` §3.1 P1 档的风险提示）。背景：slim primary 无 ifp ⇒ 该函数在 primary 上永不被调用；secondary 上静默 `return 0` 且 `ctx->mtu`（`:323`）在门禁之后 ⇒ 连本地缓存都不更新 ⇒ `ff_ifconfig mtu` **在任何进程上都是假成功**，已交付的 MTU 巨帧特性失效 | M1 | ❌ | 中（改变返回值语义，需 MTU 回归用例） | 恢复 `return 0` |
| **C18** | S | `lib/ff_msg.h`（新增 msg 类型）+ `lib/ff_dpdk_if.c:2298-2340`（`handle_msg` 分派，引自 03）+ `:294-325` 拆成"转发/执行"两函数 | 新增 | **MTU P2（完整档，可选）**：secondary 把 MTU 请求经 `msg_ring[0]` 转发给 slim primary，由 primary 调 `rte_eth_dev_set_mtu()`，成功后各 secondary 更新`ctx->mtu`；`tools/ff_ifconfig` 侧不需改（仍走 ioctl → msg）。**必须在执行前打 WARNING**：`:302-318` 的 `-EBUSY` 分支会 `dev_stop → set_mtu → dev_start`，多进程下**瞬断全体 secondary 队列**（`04` §3.1，属多进程固有代价，非 slim 引入） | C17 | ❌ | **高**（新增跨进程 msg 链路 + 全局中断操作） | revert 整串；退回 C17 的 P1 档 |
| **C19** | S | `lib/ff_dpdk_if.c:3490`（`FF_RSS_DIAG` 的 `nbq` 取值，引自 03/04） | 修改 | 1 行：把 `ff_cur_lcore_conf()->nb_queue_list[port_id]` 改为 `ff_global_cfg.dpdk.port_cfgs[port_id].nb_lcores`（全局真值）。理由：slim primary 从未执行 `:505` 的 `nb_queue_list[port_id] = pconf->nb_lcores`（已复核）⇒ 该值为 0 ⇒ `:3502` 的 `nbq > 0` 不成立 ⇒ reta 一致性诊断被静默跳过 | M1 | ❌ | 低（仅诊断路径，`FF_RSS_DIAG` 编译开关内） | 恢复 1 行 |
| **C20** | S | 无代码改动；`doc/` + 运维手册 | 文档 | **`msg_ring[0]` 归属采用 `04` §4.1 候选甲**：slim primary 精简循环天然继续消费 `msg_ring[0]`（`process_msg_ring()` 在 rx 循环**之外**，`:2803`，引自 03）⇒ **代码改动量0**，tools 默认 `ff_proc_id = 0`（`tools/compat/ff_ipc.c:42`，引自 02）保持不变、零脚本兼容性破坏。文档需明确分类：可用默认 `-p` 的（`ff_sysctl` / `ff_top` / `ff_ngctl` / `ff_ipfw`）与**必须带 `-p <收包进程 id>`** 的（`ff_ifconfig` 无 ifp、`ff_route` 无接口、`ff_traffic` 数值恒 0、`ff_knictl` 需逐进程下发） | M1 | ◐（PoC 未跑工具链，需 `08` 用例证明不挂起） | 低 | — |
| **C21** | O | `lib/ff_dpdk_if.c` 的 `handle_top_msg`（`:2186-2258` 区间，引自 04）或新增 msg 字段；`tools/` 打印 | 新增 | 在 `FF_TOP` 响应里带 `is_slim_primary` 标志位，工具在目标进程是 slim primary 时打印一行提示（例：`note: proc 0 is a slim primary (no queues/ifp); use -p <id> for interface/traffic commands`）。比"改默认 `ff_proc_id`"安全得多（`04` §4.2；候选乙会破坏 per-proc 数据采集的向后兼容） | C20 | ❌ | 低 | revert |
| **C22** | O | `lib/ff_dpdk_if.c:1189-1191` → `:354-411`（`check_all_ports_link_status()`，引自 02/04） | **不做（登记）** | N7：全库无运行期 LSC 回调（`grep link_status` 仅命中该函数体）。**本轮明确不改**（既有缺陷，非 slim 引入）。若未来要补，**只能注册在 slim primary 侧**——DPDK 官方明确"所有中断只在 primary 内触发，link status 中断在 secondary 中不工作"（`01` §1.2.5，`multi_proc_support.rst:174-177`）。`04` §3.2 已就此撤回初稿中"注册在 secondary"的错误建议 | — | — | — | — |

### 3.4 M3：KNI 归属（K4 首选 / K3-corrected 退备）（C23~C31）

> **方案选择**：据 `04` 第五次修订 + `11` Q1/Q2/Q3 代码坐实 + E4a 决定性实验，**K4（secondary 作运行时 KNI owner）为首选**，K3-corrected（K3 + Q1/Q2 纠正）为退备。K4 的 PoC 补丁 `_poc_kni_secondary.patch` 已应用于工作区源码（已复核 `ff_kni_is_runtime_owner()` 存在于 `lib/ff_dpdk_kni.c:100-106`、`owner_proc_id` 存在于 `lib/ff_config.h:342`）。
>
> **Q1 纠正（引自 `11` §1.4）**：`kni_rp` 已是 `RING_F_SC_DEQ` 单消费者（`lib/ff_dpdk_kni.c:486`，已复核），保持不动，**不需要改为多消费者**。K3-corrected 的 `kni_tx_ring` 采用 `RING_F_SP_SC`（单生产者 primary → 单消费者一个 secondary），与 `kni_rp` 的单消费者设计一致。
>
> **Q2 纠正（引自 `11` §2.5）**：`kernel_packets_ratelimit` 在 `kni_process_tx` 中只有 owner 执行（`lib/ff_dpdk_kni.c:156-165`，已复核），**天然单点限速，不需要按 `nb_lcores` 折算默认值**。`console/general_packets_ratelimit` 是每进程配额，N 个进程各自一份 ⇒ 聚合放大约 N 倍，但这不是 ratelimit 代码本身的问题，通过调整配置值即可。

| 编号 | 级别 | 文件:行号 | 类型 | 具体改法要点 | 依赖 | PoC | 风险 | 回滚 |
|---|---|---|---|---|---|---|---|---|
| **C23** | S | `lib/ff_config.h:342`（已复核，`owner_proc_id` 已存在）、`lib/ff_config.c:1087-1088`（已复核，`MATCH("kni","owner_proc_id")` 已存在）；`lib/ff_dpdk_kni.h:76`（已复核，`ff_kni_is_runtime_owner()` 声明已存在）；`lib/ff_dpdk_kni.c:100-106`（已复核，`ff_kni_is_runtime_owner()` 实现已存在） | 新增（K4） | **K4 核心改动：新增 `owner_proc_id` 配置项 + `ff_kni_is_runtime_owner()` 函数**。`owner_proc_id` 在 `[kni]` 结构体中新增 `int owner_proc_id;`（已落地）；解析 `MATCH("kni","owner_proc_id")`（已落地）；`ff_kni_is_runtime_owner()` 逻辑：`thread_mode` 时判 `rte_lcore_id() == proc_lcore[0]`，否则判 `ff_global_cfg.dpdk.proc_id == ff_global_cfg.kni.owner_proc_id`（已落地）。PoC 补丁已验证此 3 处改动 | M1 | ✅（PoC 补丁已落地） | 低（纯新增，不改既有 `ff_kni_is_owner_thread()` 语义） | 删字段/函数/解析 |
| **C24** | S | `lib/ff_dpdk_kni.c:442`（已复核，`ff_kni_alloc` 的 if 条件已改为 `ff_kni_is_owner_thread() \|\| ff_kni_is_runtime_owner()`）；`lib/ff_dpdk_kni.c:482-483`（已复核，hotplug 加了 `ff_kni_is_owner_thread()` 门禁） | 修改（K4） | **K4：让 owner secondary 也分配 `kni_stat[port_id]`**。`ff_kni_alloc()` 的 if 条件从 `ff_kni_is_owner_thread()` 改为 `ff_kni_is_owner_thread() \|\| ff_kni_is_runtime_owner()`（已落地）。但 `rte_eal_hotplug_add()` 仍**只由 primary 执行**——加 `ff_kni_is_owner_thread()` 门禁（已落地，`lib/ff_dpdk_kni.c:482-483`）。**前提**：N2 必须先修正（C32），否则 secondary 的 `nb_dev_ports` 不正确 ⇒ `kni_stat[port_id]->port_id = port_idx + nb_dev_ports`（`:487`）算错 | C23/C32 | ✅（PoC 补丁已落地） | 中（owner secondary 的 `kni_stat` 分配依赖 N2 修正） | 恢复原条件 |
| **C25** | S | `lib/ff_dpdk_if.c:822`（已复核，条件已改为 `enable_kni && rte_eal_process_type() == RTE_PROC_PRIMARY`） | 修改（K4） | **K4：`total_nb_ports *= 2` 条件改为仅 `enable_kni && rte_eal_process_type() == RTE_PROC_PRIMARY`**（已落地）。理由（`04` §2.4 第2 点）：owner 迁到 secondary 后，若仍用 `ff_kni_is_owner_thread()`，primary 的 `total_nb_ports` 不翻倍 ⇒ primary 不配置 virtio_user 口（`:1061-1148`）⇒ KNI 全废。改为仅判 primary（在 primary 上恒真）确保 primary 始终配置 virtio_user 口 | C23 | ✅（PoC 补丁已落地） | 中（改条件后 primary 始终配置 virtio_user 口，即使 `owner_proc_id` 指向 secondary） | 恢复原条件 |
| **C26** | S | `lib/ff_dpdk_if.c:2080`（已复核，广播/多播门禁已改为 `enable_kni && ff_kni_is_runtime_owner()`）；`lib/ff_dpdk_if.c:2765`（已复核，`ff_kni_process` 调用已改为 `ff_kni_is_runtime_owner()`） | 修改（K4/K3-corrected 共有） | **广播/多播门禁 + `ff_kni_process` 调用改为 `ff_kni_is_runtime_owner()`**（已落地两处）。**注意**：PoC 补丁把两处都从 `ff_kni_is_owner_thread()` 改为 `ff_kni_is_runtime_owner()`，但**仍嵌套在 rx 队列循环内**（`:2759-2768`）。正式实现还需将 `ff_kni_process()` 调用**移出 rx 队列循环**（移到 `:2803` 的 `process_msg_ring()` 附近），改为按 `cfg.dpdk.nb_ports` 遍历调用一次。**必须移出**：slim primary 的 `nb_rx_queue == 0` ⇒ 循环 0 次 ⇒ `ff_kni_process()` 一次都不调用。广播门禁用 `ff_kni_is_runtime_owner()` 而非 `!pkts_from_ring` 的理由：K4 方案下 runtime owner 是指定 secondary（有自己的 rx 队列、会进 `process_packets()`），因此 `ff_kni_is_runtime_owner()` 在 K4 下天然成立。但**若 K3-corrected 方案下 owner=primary（slim 后不进 `process_packets()`）**，则广播门禁必须改为 `!pkts_from_ring`（详见 C28） | C24/C25 | ◐（门禁改造已落地；`ff_kni_process` 移出 rx 循环未做） | 中（轮询频率下降，需 KNI 吞吐用例） | 恢复原条件 + 移回循环内 |
| **C27** | S | K3-corrected 专用：`lib/ff_dpdk_if.c:638-679`（`init_dispatch_ring()` 旁）；`lib/ff_dpdk_kni.c:190` | 新增（K3-corrected 退备） | **K3-corrected 退备方案专用**：新增 `init_kni_tx_ring()`，per-port 建 `kni_tx_ring_p%d`（`RING_F_SP_SC`：单生产者 primary → 单消费者一个 secondary），复用 `create_ring()`（`:615-635`）。把 `kni_process_rx()` L190 的 `rte_eth_tx_burst(port_id, queue_id, ...)` 改为 `rte_ring_enqueue_burst(kni_tx_ring[port_id], ...)`。指定 secondary 在 rx 循环里 dequeue 并用自己的 tx 队列发出。**K4 方案不需要此项**（owner secondary 直接用自己的 tx 队列发出，引自 `11` §3.8）。约 50 行 | M1 | ❌ | 中 | 删函数 + 恢复原调用 |
| **C28** | S | `lib/ff_dpdk_if.c:2080`（已复核） | 修改（K3-corrected 专用 / K4 验证） | **K3-corrected 下必须做**：广播/多播门禁从 `ff_kni_is_runtime_owner()` 改为 `!pkts_from_ring`。理由：K3-corrected 的 owner=primary（slim 后不进 `process_packets()`），`ff_kni_is_runtime_owner()` 永不成立 ⇒ 广播/多播永不入 KNI。`!pkts_from_ring` 精确等价于"我是从线上收到该包的那个进程"⇒ 恰好一份。**K4 方案下**：PoC 补丁已用 `ff_kni_is_runtime_owner()`（owner=secondary 会进 `process_packets()`），但需验证在多 secondary 场景下是否恰好一份——若 owner secondary 也在 `pkts_from_ring=1` 路径收到广播包则不入队（正确），但在 `pkts_from_ring=0` 路径收到则入队（也正确，恰好一份）。**E4d 用例必须验证** | C26 | ◐（K4 用 `ff_kni_is_runtime_owner()` 已落地；K3-corrected 改 `!pkts_from_ring` 未做） | **高**（份数语义极易搞错，必须有"恰好一份"的用例） | 恢复原条件 |
| **C29** | S | `lib/ff_config.c:1419-1435`（已复核） | 修改 | 放开 C05 的拒绝：`primary_slim=1` 时**不再要求** primary 在 `lcore_list` 中。K4 方案改为要求 `proc_lcore[cfg->kni.owner_proc_id]` 在某个 port 的 `lcore_list` 中（即 owner secondary 必须持有物理口队列）；K3-corrected 改为要求至少有一个 secondary（`pc->nb_lcores >= 1`）来承担 `kni_tx_ring` 消费与入向收包。理由：B2 校验的目的是"保证 primary 有队列以驱动 `ff_kni_process()`"，K4/K3-corrected 让该函数不再需要 primary 有队列 ⇒ **校验前提消失**。约 8 行 | C23~C28 | ❌ | 中（放宽校验，需 UT 覆盖新旧两种组合） | 恢复 C05 的拒绝 |
| **C30** | S | `lib/ff_dpdk_if.c:535`（已复核） | 修改 | K3-corrected：视 `kni_tx_ring` 深度决定是否计入 mbuf 预算（现有公式已含 `nb_ports * KNI_MBUF_MAX + nb_ports * KNI_QUEUE_SIZE`，`:543-546`，已复核，可复用其预算）。**K4 方案不需要此项**（不新增 ring）。K4 下 mbuf 公式不变。约 0~2 行 | C10/C27 | ❌ | 低 | 恢复 |
| **C31** | O | `config.ini:307` 起的 `[kni]` 注释块（已复核，当前整段注释掉）+ `doc/`；新增 `[kni] owner_proc_id` 注释说明 | 文档/默认值 | **据 Q2 纠正（引自 `11` §2.5）**：`kernel_packets_ratelimit` 在 `kni_process_tx` 中只有 owner 执行，**天然单点限速，不需要按 `nb_lcores` 折算默认值**。`console/general_packets_ratelimit` 是每进程配额（`lib/ff_config.h:44` 注释自述），广播门禁改造后入队者从 1 个进程变为 N 个进程 ⇒ 聚合放大约 N 倍。处置：把配置语义在文档中明确为"**每进程**"（不折算默认值，由用户显式配置）。新增 `owner_proc_id` 配置说明（默认 0=primary 保持现状；设为 secondary 的 proc_id 即启用 K4）。需用例验证内核侧不被压垮 | C28 | ❌ | 中（不处理会让内核侧被 N 倍流量打） | — |

### 3.5 M4：加固与清理（C32~C38）

| 编号 | 级别 | 文件:行号 | 类型 | 具体改法要点 | 依赖 | PoC | 风险 | 回滚 |
|---|---|---|---|---|---|---|---|---|
| **C32** | K4 前置必做（原 O 级，K4 方案下升级） | `lib/ff_dpdk_if.c:79`（引自 02）、`:421-426`（已复核）、`:758`、`:844`（引自 04）；`lib/ff_dpdk_kni.c:487`（已复核，原 `:478`） | 修改 | **N2 修正，采用 `04` §5.2 候选丙**：primary 在 hotplug 之前把 `rte_eth_dev_count_avail()` 快照 `rte_memzone_reserve("ff_nb_dev_ports", ...)` 发布；secondary `rte_memzone_lookup()` 读取，失败退回本地统计并打 WARNING（向后兼容）。`:79` 的注释 `/* primary is correct, secondary is not correct, but no impact now*/` 可删。`:487` / `:844` **无需改代码**，取值自动变正确。**K4 前置必做理由**（引自 `11` §3.5）：owner secondary 需要 `nb_dev_ports` 算 `kni_stat[port_id]->port_id = port_idx + nb_dev_ports`（`:487`），若不修正则指向错误 port ⇒ KNI 收发打到错误设备。**K3-corrected 下可延后**（`nb_dev_ports` 仍只在 primary 侧使用） | M1 | ❌ | 中（新增共享对象 + lookup 失败分支） | revert；K3-corrected 路线下不影响功能 |
| **C33** | O | `lib/ff_dpdk_if.c:1745`（`init_flow(0, 80)`）、`:1777`（`fdir_add_tcp_flow(...)`）（引自 02/04） | 新增 | 各补 `if (rte_eal_process_type() == RTE_PROC_PRIMARY)` 包裹，与相邻的 `:1707`（`FF_FLOW_ISOLATE`）、`:1760`（`FF_FDIR`）门禁对齐。各 2 行。属**既有缺陷**（同一函数内 4 个 rte_flow 调用，2 个有门禁 2 个没有），slim 会因secondary 数增加而放大重复安装/告警 | M1 | ❌ | 低（在编译开关内） | 删门禁 |
| **C34** | O | `lib/ff_dpdk_if.c:756-781`（`init_kni()`，引自 02/04） | 修改/删除 | **N8**：同名变量 `nb_ports` 先取 `nb_dev_ports`（`:758`）、中途在 `:773` 被重新赋值为 `ff_global_cfg.dpdk.nb_ports` ⇒ 建议重命名为 `nb_dev_ports_snapshot` / `nb_cfg_ports`；`:770-771` 的 `struct rte_mempool *mbuf_pool = pktmbuf_pool[socket_id];` **声明后从未使用**（`ff_kni_alloc()` 不接收 mempool 参数）⇒ 死代码，删除 | C32 | ❌ | 低 | revert |
| **C35** | O | `lib/ff_dpdk_if.c:531-534`（已复核） | 修改 | 把 `nb_lcores` 语义拆成「总进程数」（用于 TX/cache/dispatch 项）与「收包进程数」（用于 RX 项）两个局部变量。slim 下前者比后者大 1 ⇒ TX/cache/dispatch 略微超配（无害，`02` B3 注 2），拆开可省这部分内存 | C10 | ❌ | 低 | 恢复单变量 |
| **C36** | O | `lib/ff_api.h`（新增声明）+ `lib/ff_dpdk_if.c`（实现） | 新增 | 新增只读 API `int ff_is_slim_primary(void);` 供 app 识别自身角色。**必要性**：`03` §2.4 裁决保留用户 loop 回调 `lr->loop(lr->arg)`（`:2817-2820`，已复核）⇒ app 在 slim primary 上会空转（socket 建得起来但永远收不到事件）。当前替代手段是让 app 通过 `ff_rss_self_queue_info()` 返回 -1（`:3212-3213`，引自 03）自行识别，语义晦涩。**新增对外 API 需按最小注释规约在头文件写清契约** | M1 | ❌ | 低（纯新增，不改既有行为） | 删API |
| **C37** | O | `lib/ff_veth.c` 的 `ff_veth_attach()`（`:1153-1154`，引自 03） | 新增 | slim primary 下 `ff_veth_attach()`被调用即 `rte_exit`（或 `ff_log(ERROR)` + 返回 NULL）。**目的**：把 `03` R3 的残余风险（"若 app 或未来改动误调 `ff_veth_attach`，tx-queue-0 跨进程竞争风险复现"）变成 fail-fast。断言而非静默容忍 | C14 | ❌ | 低 | 删断言 |
| **C38** | O | `doc/F-Stack_Release_Note.md`、`doc/F-Stack_Development_Guide.md`、`config.ini` 注释 | 文档 | ① Release note 显著标注**语义变更**：老运维脚本若默认对 proc 0 做 `ff_ifconfig`，行为从"生效"变为"无接口"（`03` R6）；② 运维手册写入 C20 的工具分类表、C12 的降级态处置流程（**必须择机全组重启**：停全部进程 → 清理 `/var/run/dpdk/rte/` → 全组重启；primary **不可原地拉回**，`05` E10）；③ 明确"primary-slim 的收益是稳定性，**不包含 CPU 资源节省**"（`06` §4.4，纠正"瘦身 = 省一个核"的预期） | M1~M3 | — | 低 | — |

---

## 四、配置项落地清单

### 4.1 `[dpdk] primary_slim`

| 项 | 值 |
|---|---|
| section /键名 | `[dpdk]` / `primary_slim` |
| 取值语义 | `int`。`0` = 关闭（行为与改造前**完全一致**）；`1` = primary 常驻但不分配任何 rx/tx 队列，只做设备配置 / 共享对象创建 / 控制面 |
| 默认值 | **`0`**（必须默认关闭，保证零回归） |
| 生效范围 | 仅 `proc_type=primary` 且 `thread_mode=0` 且 `nb_procs >= 2` |
| 校验规则 | V2（C04）slim 时 primary lcore 不得出现在任何 `[portN].lcore_list`；V3（C05）与 `kni.enable=1` 互斥（M3 后由 C29 放开）；V4（C06）与 `thread_mode=1` 互斥；V5（C07）`nb_procs >= 2`；V6（C08）slim 生效打 INFO、`nb_procs < 3` 打 WARNING |
| 结构体字段 | `lib/ff_config.h:295-296` 旁新增 `int primary_slim;`（已复核该区段为 `thread_mode`/`nb_threads`） |
| 解析代码位置 | `lib/ff_config.c:1035-1036` 的 `MATCH("dpdk","thread_mode")` 旁（已复核） |
| 默认值代码位置 | `lib/ff_config.c:1508-1519` 的 `ff_default_config()`（已复核；`memset` 已保证 0，显式赋值仅为可读性） |
| 校验代码位置 | `lib/ff_config.c:1414` 之后（V2）、`:1419` 之前（V3）、`:1484` 之前（V4/V5/V6），均已复核 |
| 文档更新位置 | `config.ini`的 `[dpdk]` 段（建议紧随 `:6-8` 的 `thread_mode` 注释块，已复核）；`config.ini:189` 的 `#lcore_list=0` 附近补"slim 时必须显式配置并排除 primary lcore"；`doc/F-Stack_Release_Note.md` |
| 特别注意 | `config.ini` 属交付配置文件。**只提交本特性相关的注释块与注释掉的示例项**，严禁带入`lcore_mask` / `idle_sleep` / `[portN]` 本机 IP 等本地测试值；`git add` 时不选中带本地改动的行（本地值保留在未暂存区供运行测试） |
| 陷阱 | `config.ini:189` 的 `lcore_list` 默认是**注释掉的**（已复核），此时 `pconf->nb_lcores = nb_procs`、`lcore_list` 全量继承 `proc_lcore`（引自 03§2.1.3）⇒ **`primary_slim=1` 必须显式写 `lcore_list`**，否则 primary 被自动放回列表，V2 会报错（这是期望行为，不能静默） |

### 4.2 `[dpdk] primary_slim_idle_sleep`

| 项 | 值 |
|---|---|
| section / 键名 | `[dpdk]` / `primary_slim_idle_sleep` |
| 取值语义 | `unsigned`，微秒。slim primary 空闲时的休眠时长，替代`idle_sleep`（仅对 slim primary 生效） |
| 默认值 | **`1000`（1ms）** |
| 生效范围 | 仅 `primary_slim=1` 且本进程为 primary |
| 校验规则 | 建议 `> 0`；若配置为 0 则打 WARNING 说明会退化为空转烧核（不强制拒绝，留给排障场景）。上限建议在文档中提示"过大会让 `ff_sysctl`/`ff_top` 响应变慢"（`03` 未决问题 4） |
| 结构体字段 | `lib/ff_config.h:311-312` 旁新增 `unsigned primary_slim_idle_sleep;`（已复核该处为 `idle_sleep`） |
| 解析代码位置 | `lib/ff_config.c:1072-1073` 的 `MATCH("dpdk","idle_sleep")` 旁（已复核） |
| 默认值代码位置 | `lib/ff_config.c`的 `ff_default_config()`（**必须显式置 1000**，否则 memset 后为 0 ⇒ C13 失效） |
| 使用点| `lib/ff_dpdk_if.c:2822-2828`（已复核） |
| 文档更新位置 | `config.ini` 的 `[dpdk]` 段（紧随 `primary_slim`）；说明必须写明"仅对 slim primary 生效，普通进程仍用 `idle_sleep`（`config.ini:51`）" |
| 依据 | `06` §4.4 P2 实测：PoC 的 slim primary 占 CPU **99.8%**，与"瘦身"初衷相悖，且会与同 NUMA 的 secondary 争cache/内存带宽 |

### 4.3 `[kni] owner_proc_id`（K4 方案新增，已落地）

| 项 | 内容 |
|---|---|
| section / 键名 | `[kni]` / `owner_proc_id` |
| 取值语义 | `int`。`0` = primary（保持现状，与 `ff_kni_is_owner_thread()` 等价）；设为 secondary 的 proc_id（如 `1`）即启用 K4 方案——该 secondary 作运行时 KNI owner |
| 默认值 | **`0`**（必须默认关闭 K4，保证零回归） |
| 生效范围 | 仅 `kni.enable=1` 且 `proc_type` 对应的进程 |
| 结构体字段 | `lib/ff_config.h:342`（已复核，`int owner_proc_id;` 已存在于 kni 结构体） |
| 解析代码位置 | `lib/ff_config.c:1087-1088`（已复核，`MATCH("kni","owner_proc_id")` 已存在） |
| 使用点 | `lib/ff_dpdk_kni.c:100-106`（已复核，`ff_kni_is_runtime_owner()` 已实现：`thread_mode` 时判 `rte_lcore_id() == proc_lcore[0]`，否则判 `ff_global_cfg.dpdk.proc_id == ff_global_cfg.kni.owner_proc_id`） |
| 文档更新位置 | `config.ini:307` 起的 `[kni]` 注释块（当前整段注释掉，已复核）；新增 `owner_proc_id` 说明 |
| 依据 | `04` §2.4 K4 方案（首选）+ `11` §3.8 关键发现 + E4a 决定性实验（已实测 secondary 能发现、probe virtio_user 口并访问共享 KNI ring） |
| PoC 状态 | **已落地**：`_poc_kni_secondary.patch` 已应用于工作区源码（7 处改动全部到位） |

---

## 五、依赖顺序与并行度（DAG）

### 5.1 依赖图

```
[M1 关键路径]
C01 ──┬─> C02 ──> C04 ──> C05 ──> C09 ══> C10 ──> C11 ──> C12
      │                ▲
      ├─> C03 ─────────────────────────────┼──> C13
      ├─> C06 ──> C07 ──> C08              │
      ├─> C15 (仅注释, 可任意时刻)          │
      └─> C16 (config.ini 注释, 可并行)     └──> C14

  ══>  =必须同批提交（C09 单独上会造成 mbuf 池欠配）

[M2]  M1 ──> C17 ──> C18(可选)
      M1 ──> C19            (与 C17 并行)
      M1 ──> C20 ──> C21    (与 C17/C19 并行)
      C22 = 不做，仅登记

[M3 K4 首选路径]
      C32(N2前置) ──> C23 ──> C24 ──> C25 ──> C26 ──> C28 ──> C29
                                                              │
                                          C28 ──> C31

[M3 K3-corrected 退备路径]
      M1 ──> C27 ──> C24(K3版) ──> C26(K3版) ──> C28(改!pkts_from_ring) ──> C29
      C10 + C27 ──> C30
      C28 ──> C31

[M4]  M1 ──> C34     (C33/C35/C36/C37 各自独立; C32 已移至 M3 K4 前置)
      C10 ──> C35
      C14 ──> C37
      M1~M3 ──> C38(文档收口, 最后做)
```

### 5.2 关键路径与并行度

| 项 | 内容 |
|---|---|
| **关键路径** | `C01 → C02 → C04 → C05 → C09+C10 → C11 → C12`（M1 主干），随后 K4 路径：`C32(N2 前置) → C23 → C24 → C25 → C26 → C28 → C29`（KNI 主干）。这两条串是全部工作的最长链 |
| **必须严格串行** | ① C01 先于所有（结构体字段是一切门控的前提）；② C09 与 C10 **同批**；③ C06 必须插在 `ff_config.c:1484` 之前（`:1496-1498` 会改写 `nb_procs`）；④ **K4 方案下 C32（N2 修正）必须先于 C24**（owner secondary 的 `kni_stat[port_id]->port_id` 依赖正确的 `nb_dev_ports`）；⑤ C23 → C24 → C25 → C26（K4 代码改动连锁）；⑥ C34 依赖 C32（变量重命名依赖 N2 语义确定） |
| **可完全并行** | ① C15、C16（无逻辑依赖）；② M2 的三条支线 C17 / C19 / C20 互不相干；③ M4 的 C33、C35、C36、C37 四点彼此独立；④ **M2 与 M3 可由两人并行**（唯一潜在冲突文件是 `lib/ff_dpdk_if.c`，但改动区段不重叠：M2 在 `:294-325` / `:2298-2340` / `:3490`，M3 在 `:638-679` / `:2080` / `:2756-2801`） |
| **建议并行度** | M1 单人串行（1人）；M2 + M3 双线并行（2 人）；M4 可穿插在任何空隙 |

---

## 六、风险与回滚

### 6.1 回滚点

| 编号 | 回滚点 | 覆盖范围 | 回滚代价 | 备注 |
|---|---|---|---|---|
| **R0** | **配置级回滚** | 全部 | **≈0**（一行配置 + 恢复 `lcore_list` 注释，无需重编译、无需改代码） | `primary_slim=0` ⇒ 行为与改造前逐字一致。这是选显式开关（`03` §2.1.1 候选乙）的直接收益 |
| **R1** | C01~C16（M1） | 配置项 + 校验 + B1/B3 + 退出门控 + idle_sleep | 中：需revert 1 个提交串（4 文件），重新 clean build | 此时未触碰 `main_loop` 的 KNI 与 MTU 路径 |
| **R2** | C17~C22（M2） | MTU 归属 + RSS 诊断 + tools 提示 | 低：三条支线可**分别** revert | 回退后 MTU 退回"静默假成功"（可见性丢失，数据面不受影响） |
| **R3** | C23~C31（M3） | KNI（K4 首选含 C32 前置 / K3-corrected 退备） | 中高：9 点连锁 + K4 含 N2 修正，需整串 revert **并恢复 C05 的配置期拒绝** | 独立提交串，不影响 R1/R2。K4 方案 revert 后还需恢复 `ff_kni_is_runtime_owner()` 相关改动 |

### 6.2 重点风险（含两个被点名项）

| 编号 | 风险 | 相关改动 | 影响 | 缓解 |
|---|---|---|---|---|
| **RK-1（`03` R3，重点）** | **tx-queue-0 跨进程竞争**：若给 slim primary attach ifp，其 `tx_queue_id[port]` 恒为 0（`:501` 的赋值从未执行，已复核）⇒ `send_burst()` 会 `rte_eth_tx_burst(port, 0, ...)`（`:2378-2389`，引自 03）撞进 `lcore_list[0]` 那个 secondary 的 tx 队列，而 DPDK tx 队列**非 MT-safe** | C14（裁决不attach）、C37（断言） | tx 队列结构损坏 / 丢包 / 崩溃，且是**跨进程**故障、排查极难。触发源很隐蔽：primary 一旦持 ifp，任何本机发起的报文（ARP、ICMP、IPv6 NS-DAD、MLD、路由/接口操作副作用）都会走到这条路径 | ① C14 明确"不 attach ifp、代码不改、只加日志"；② C37 加断言让误调fail-fast；③在 `07`/`08`/运维文档三处重复警示；④ **若未来必须给 primary ifp，则必须先给它分配专属 tx 队列（= 退化为 S3）或引入 tx 加锁** |
| **RK-2（`06` P2，重点）** | **slim primary 仍占满一个 CPU 核（实测 99.8%）**：`main_loop` 紧密空转跑 timer 与 `msg_ring`，而 `config.ini:51` 默认 `idle_sleep=0`（已复核） | C13、C03、C08 | 白烧一个物理核；与同 NUMA 的 secondary 争 cache / 内存带宽；且会让用户"瘦身 = 省一个核"的预期落空| ① C13 引入 `primary_slim_idle_sleep`（默认 1000us）；② C03 **必须显式置默认值**（漏置则 memset 后为 0 ⇒改动完全失效，这是最容易埋的雷）；③ C08 在 INFO 中打印生效值便于运维核对；④ `08` 的 IT-RT-06 用例把"CPU ≤ 阈值"设为**阻塞发布**的验收项；⑤ C38 在文档中纠正资源节省预期 |
| RK-3（`02` N4 / `03` R2） | slim primary 优雅退出执行 `rte_eal_cleanup()` ⇒ 全体 secondary 共享内存被拆| C11、C12 | 大概率全组崩。E2/E2e 的乐观结论只覆盖**强杀**路径 | C11 门控跳过；C12 告警；C38 写入"slim primary 常驻、不得优雅退出"的运维规则；`08` 的 E8 用例实测该分支 |
| RK-4（`03` R4 / `02` N5） | `ff_ifconfig mtu` 假成功⇒ 已交付的 MTU巨帧特性在 slim 下失效 | C17/C18 | 用户以为改了 MTU、硬件实际没变，各 secondary `ctx->mtu` 与硬件不一致 | C17（P1，门控返回 `EPERM`，失败可见）必做；C18（P2）可选，但需警示 `-EBUSY` 分支会瞬断全体 secondary 队列 |
| RK-5（`04` §2.3 第5 点 / `11` §2.5 Q2 纠正） | C28 的广播/多播门禁改错 ⇒ 要么广播完全不进内核，要么内核收到 **N 份**重复 | C28 |ARP/DHCP/NS-RA-MLD/OSPF 失效，或内核被N 倍广播压垮 | **K3-corrected**：必须用 `!pkts_from_ring` 而非仅 `enable_kni`（owner=primary slim 后不进 `process_packets()`）；**K4**：PoC 补丁用 `ff_kni_is_runtime_owner()`（owner=secondary 会进 `process_packets()`），需 E4d 验证恰好一份。`console/general_packets_ratelimit` 聚合放大约 N 倍但 `kernel_packets_ratelimit` 天然单点限速（`11` §2.5 Q2 纠正，不需折算）；`08` 设"恰好一份"的计数用例 |
| RK-6（`03` R11） | 单 secondary（`nb_queues == 1`）时 `set_rss_table()` 被跳过、NIC 保持PMD 默认 reta | C07/C08 | 若默认 reta 不全指 queue 0 ⇒ **全量丢包** | C08 对 `nb_procs < 3` 打 WARNING；`05` E5+ E3（PoC 在 `nb_queues=1` 且持队列者为 secondary 时12/12 通过）已给出virtio 上的正面证据，但**非 virtio PMD需复测**（`08` 列为环境受限项） |
| RK-7（`03` R12） | `FF_FLOW_ISOLATE` / `FF_FLOW_IPIP` 编译时 `set_rss_table()` 整体被编译掉（`:785-810` 的 `#if !defined(...)`，引自 03） | 全局 | "reta 自动收缩 ⇒ 无孤儿队列"的论证在该组合下**不适用** | `08` 把这两个编译组合列为独立回归用例；文档中限定 S1 孤儿队列结论的适用范围 |
| RK-8（`03` R13） | 各进程用独立 `config.ini` 时 `primary_slim` 取值不一致 ⇒ 池公式 / msg 归属 / KNI 判定在进程间分叉 | C08 | 难以排查的偏差 | C08 的 INFO 日志供对账；**代码层无法强制一致**（无跨进程配置校验通道），只能靠运维规范 |
| RK-9（`03` R14） | primary 崩溃后进程组进入**控制面降级态**且**不可就地修复**（`05` E10 实测 primary 不可原地拉回） | 全局（方案固有） | 失去 IPC 服务端 / 扩堆代理 / 中断处理 / KNI / 部分工具链能力 | 非本文可解。C38 写入"必须择机做一次计划内全组重启"的流程；`03` 未决问题 10 建议未来补"降级态检测 + 主动告警" |

---

## 七、工作量估算

> 口径：**小** = ≤ 半人日（含自测）；**中** = 0.5~2 人日；**大** = 2~5 人日。估算依据：改动行数（来自 `03`/`04` 的估计量）、是否新增跨进程机制、是否需要实机验证、是否有 PoC 先例。

| 里程碑 | 改动点 | 估算 | 依据 |
|---|---|---|---|
| **M1** | C01~C08（配置 + 校验，8 点） | **中（约 1.5 人日）** | 纯配置解析与校验，模式可完全照抄既有 `thread_mode`（`:1035-1036`）与 KNI 校验（`:1419-1435`）；主要成本在 8 条新单测 + 7~8 个新 fixture |
| | C09、C10（B1 + B3，2 点） | **小（约 0.5 人日）** | **PoC 已实测跑通**，正式实现只是加一层`primary_slim` 门控 + 把近似式换成精确式；改动共约 6 行 |
| | C11、C12（退出语义，2 点） | **中（约 1 人日）** | 代码量小（约 8 行）但**全新未验证**，且需实机跑 E8（优雅退出）才能确认；valgrind/`make check` 需复验非 slim 路径未被误门控 |
| | C13（idle_sleep，1 点） | **中（约 1 人日）** | 代码约 5 行，但需实机复测 CPU 占用（E12）并量化 1ms 对 `ff_sysctl`/`ff_top` 响应的影响 |
| | C14~C16（日志/注释/配置文档，3 点） | **小（约 0.5 人日）** | 无逻辑改动 |
| | **M1 小计** | **约 4.5 人日** | 含clean build + 单测 + 一轮实机验证（PoC 已铺路，风险最低的批次） |
| **M2** | C17（MTU P1） | **小（约 0.5 人日）** | 2 行 + MTU 回归用例 |
| | C18（MTU P2，可选） | **大（约 3人日）** | 新增 msg 类型 + `handle_msg` 分派 + 函数拆分 + 跨进程时序 + `dev_stop/start` 瞬断风险的实机验证 |
| | C19（RSS 诊断） | **小（约 0.2 人日）** | 1 行 |
| | C20、C21（tools 归属） | **小~中（约 1 人日）** | C20 代码 0 行（纯文档 + 用例证明）；C21 需改 msg 结构与工具打印 |
| | **M2 小计** | **约 1.7 人日（不含 P2）／约 4.7 人日（含 P2）** | P2 是否做取决于"运行期改 MTU"的实际需求 |
| **M3** | C23~C31（KNI K4 首选 / K3-corrected 退备，9 点） | **大（约 5 人日）** | K4 方案 PoC 补丁已落地 7 处改动（`_poc_kni_secondary.patch`），剩余 `ff_kni_process` 移出 rx 循环 + B2 校验调整 + N2 修正 + E4b~E4f 实机验证；K3-corrected 需新增 `kni_tx_ring`（约 50 行）。两方案共有：**广播/多播份数语义变更**（RK-5，最易错）、KNI 双向连通与"恰好一份"的实机验证；且本机 `[kni]` 段当前注释掉（`config.ini:307`，已复核）⇒ **需先搭 KNI 测试环境**，这部分环境成本可能超过编码成本 |
| **M4** | C34~C38（C32 已移至 M3 K4 前置） | **小~中（约 2.3 人日）** | C33~C35 各 1~4 行（C33 需编译组合验证）；C36/C37 纯新增；C38 文档收口 |
| **合计** | 38 点 | **约 13.5 人日（不含 MTU P2）／约 16.5 人日（含 P2）** | C32 从 M4 移至 M3（K4 前置），M4 估算相应调减。**不含** `08` 的测试代码实现与实机测试执行工时；不含 KNI 测试环境搭建；不含评审/打回返工 |

**估算的诚实边界**：M1 因有 PoC 先例，估算可信度较高；M3（KNI）的最大不确定性是**测试环境**——`05` §11.6 已记录本轮 KNI 场景完全未覆盖（`[kni]` 段未配置），E4/E13 都待做，若 KNI 环境搭建受阻，M3 可能显著超出估算。

---

## 八、不做项清单（范围边界）

> 目的：防止范围膨胀。以下项**明确不在本方案范围内**，理由均有代码或实测依据。

| 编号 | 不做项 | 理由 |
|---|---|---|
| **X1** | **primary 热切换 / primary failover**（选举新primary 接管共享内存所有权） | DPDK 模型固有约束：primary 是 IPC 唯一服务端（`01` §1.2.1，`eal_common_proc.c:750-751`）、全体 secondary 的扩堆代理（§1.2.2，`malloc_heap.c:484`/`:1030`）、唯一能收中断的进程（§1.2.5）。且 `05` E10 已实测 **primary 不可原地拉回**（`Cannot init memzone`，exit 1），因为 `/var/run/dpdk/rte/` 的 `config`/`fbarray_memzone` 仍被存量 secondary 持有映射 |
| **X2** | **连接迁移 / 连接接管**（把 primary 队列上的连接迁到secondary） | S1 从根上让 primary **不持有任何队列** ⇒ 不存在需要迁移的连接（`02` B4 + D.3.2 代码坐实、`05` E3b 实测 12/12 零中断）。做迁移是解决一个已被消除的问题 |
| **X3** | **运行期队列重配 / 动态增删 secondary 并调整 `nb_queues`** | `nb_queues = pconf->nb_lcores`（引自 03）在`init_port_start()` 一次性决定；运行期改需 `dev_stop/dev_configure/dev_start`，多进程下会瞬断全体 secondary（同 `04` §3.1 对 MTU `-EBUSY` 分支的判断） |
| **X4** | **KNI 完整重写 / KNI owner failover** | `04` §2.4 已论证：K4 把 runtime owner 放在指定 secondary，该 secondary 崩溃则 KNI 中断但**只需重启该 secondary 即可恢复**（可原地重启，`03` E2e 已证），无需为 KNI 单独设计 failover；K3-corrected 把 owner 留在 primary，KNI 能力的可用期恰好与控制面其他能力**同生共死**。两方案均无需为 KNI 单独设计 failover |
| **X5** | **消除 `igb_uio` 的 refcnt 语义**（让"所有进程都退出"后设备仍可用） | 内核模块行为（FP-1：`igb_uio.c:352-366` 的 `atomic_dec_and_test(&udev->refcnt)` → `pci_clear_master()`，引自 02/03），f-stack 侧无法消除。这是 S1 的**固有边界**（`03` §6.1 第 3 条） |
| **X6** | **降级态自动检测与主动告警** | `03` 未决问题 10 明确"本轮不设计"，仅记录必要性（候选思路：secondary 周期性探测 `mp_socket` 可连通性或轻量 `rte_mp_request_sync()` 探活）。属后续里程碑 |
| **X7** | **运行期 link status（LSC）感知** | C22：N7 是**既有**缺陷（全库无运行期 LSC 回调），非 slim 引入；若补只能补在 slim primary 侧（`01` §1.2.5）。本轮仅登记 |
| **X8** | **in-flight msg 对象泄漏修复** | `02` 开放问题 5：primary 被强杀时其持有的 msg 对象永久泄漏（消费者才 `rte_mempool_put`）。`03` §6.2 明确 S1 不改善此项 |
| **X9** | **S3（primary 保留最小队列）与 S4（`thread_mode=1`）的落地** | S3 是 `kni.enable=1` 且不接受 KNI 改造风险时的**兜底备选**（= `04` 的 K1），本方案走 K3；S4 是与 #1078 正交的长期方向，且故障域反向放大（一挂全挂、单进程退出即 refcnt 归零） |
| **X10** | **S5（外部守护+ 分层恢复）的实现** | `03` §3.1 定位为**配套建议（兜底）**、0 个 f-stack 文件改动、纯外部编排。属运维侧工作，不在本编码分解内（但 C38 需在文档中给出它要自动化的那次"计划内全组重启"流程） |
| **X11** | **为本特性额外预留 hugepage** | `03` §2.3 改动 #2 已论证：C10 修好公式后内存占用相对现状**不增加**（同构配置等值，slim 配置略降） |
| **X12** | **修改 `tools/` 的默认 `ff_proc_id`** | `04` §4.1 候选乙已被否决：会破坏 `ff_top`/`ff_traffic`/`ff_sysctl` 等 per-proc 数据采集的向后兼容且用户不自知；改用 C21 的提示方案|

---

## 九、未决问题

| 编号 | 未决问题 | 影响的改动点 | 建议处置 |
|---|---|---|---|
| **U1** | **`ff_freebsd_init()` 在无 ifp 情况下能否完整跑完**（`02` 开放问题 4 / `03` §7.2 第 1 条）。PoC 的 E3 显示 primary 的 FreeBSD 栈初始化完成，但未穷尽全路径 | C14 | `08` 的 IT-1078-01/IT-RT-01 显式断言 primary 日志中栈初始化完成、无 `rte_exit`；长稳用例（E9）观察是否累积异常状态 |
| **U2** | **单 secondary（`nb_queues == 1`）时 PMD 默认 reta 是否全指 queue 0**（`03` R11 / `02` 开放问题 3）。E5 + E3 在**virtio** 上给出正面证据，`ixgbe`/`i40e`/`mlx5`/`ice` 未验证 | C07/C08 | 本机仅 virtio（`05` §1 已声明环境限制）⇒ 列为**环境受限项**；C08 的 `nb_procs < 3` WARNING 是当前唯一缓解 |
| **U3** | **`FF_FLOW_ISOLATE` / `FF_FLOW_IPIP` 编译组合下孤儿队列是否仍被消除**（`03` R12/§7.2 第 3 条）。需确认 `create_tcp_flow()`/`create_ipip_flow()` 的规则集是否覆盖全部入向流量 | 全局结论适用范围 | `08` 列为独立编译组合回归用例；文档中限定结论适用范围 |
| **U4** | **`primary_slim_idle_sleep = 1000us` 是否会让 `ff_sysctl`/`ff_top` 响应变慢到用户可感知**（`03` §7.2 第 4 条） | C13 | `08` 给出量化用例（工具响应时延 P50/P99），据实测调整默认值 |
| **U5** | **slim primary 的协议栈实例是否会随时间累积状态**（无 ifp 的栈上执行 route 操作产生的中间态；与 `02` 开放问题 5 合并考察） | C11/C20 | `08` 的 E9 小时级长稳用例 + `message_pool`/mbuf 水位观测 |
| **U6** | **多 NUMA / 多 port 异构 `lcore_list` 下C10 新公式的实际表现**（`03` §2.6 等值性论证只覆盖同构配置） | C10 | `08` 设多port 异构 `lcore_list` 的池规模用例；本机单port ⇒ 部分为环境受限项 |
| **U7** | **`tools/` 下每个工具 main 的深层 primary 依赖**（`02` 开放问题 6 / `03` §7.2 第 7 条，本轮未逐个通读 8 个工具的 main） | C20/C21 | `08` 的 IT-RT-11 逐工具跑一遍（`ff_sysctl`/`ff_top`/`ff_ifconfig`/`ff_route`/`ff_traffic`/`ff_ipfw`/`ff_ngctl`/`ff_knictl`） |
| **U8** | **E8（primary 优雅退出 + refcnt −1 的叠加效应）未实测**（`03` §7.2 第 12 条 / `05` §13 E8 待做）。E2e/E10 均为强杀场景 | C11/C12 | `08` 的 IT-RT-08 为 M1 的**阻塞发布**验收项 |
| **U9** | **C18（MTU P2）是否要做**——取决于"运行期改 MTU"是否为真实需求；若做，`-EBUSY` 分支会瞬断全体 secondary 队列 | C18 | 需产品/运维侧决策，本文列为可选。默认只做 C17（P1） |
| **U10** | **M3 的 KNI 测试环境可用性**：本机 `[kni]` 段整段注释（`config.ini:307`，已复核）。E4a 已实测通过（secondary 发现/probe virtio_user 口），但 E4b~E4f 待做 | M3 全部 | E4a 已证明 KNI 环境可用。建议 M3 启动后优先跑 E4b（KNI 功能基线） |
| **U11** | **~~C23 的 ring 消费者模型选择~~**（已关闭，据 `11` Q1 纠正）：`kni_rp` 已是 `RING_F_SC_DEQ` 单消费者，正确且充分，不需要改为多消费者。K3-corrected 的 `kni_tx_ring` 采用 `RING_F_SP_SC`（单生产者 primary → 单消费者一个 secondary），与 `kni_rp` 的单消费者设计一致。K4 不需要 `kni_tx_ring` | C23/C27 | 已关闭 |
| **U12** | **~~C31 的 ratelimit 默认值折算方式~~**（已关闭，据 `11` Q2 纠正）：`kernel_packets_ratelimit` 在 `kni_process_tx` 中只有 owner 执行，天然单点限速，**不需要按 `nb_lcores` 折算默认值**。`console/general_packets_ratelimit` 是每进程配额，N 个进程各自一份 ⇒ 聚合放大约 N 倍，但这不是 ratelimit 代码本身的问题，通过调整配置值即可。倾向"只改文档语义、不动默认值"，由用户显式配置 | C31 | 已关闭 |

---

> **配套文档**：测试规格见 `08-测试规格.md`（单测用例与 fixture、集成用例、性能基线规格、回归范围、验收标准汇总表）。
