; Stage-6 Phase-2 fixture: [rss_check] but no [portN] / [vlanN] sections set
; up port_cfgs/vlan_cfgs first -> rss_check_cfg_handler returns 0 early.
[dpdk]
lcore_mask=1
channel=4
port_list=0

[rss_check]
enable=1
